{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"Twilio Agent Connect Google         Google Cloud integrations for Twilio Agent Connect \u2014 connect Google Cloud agent services to Twilio's communication channels.    <p> Documentation     \u25c6 Python SDK     \u25c6 Examples     \u25c6 Deployment </p> <p>Google Cloud-specific connectors for Twilio Agent Connect (TAC) \u2014 Twilio's framework for connecting AI agents to voice and messaging channels (Python SDK) \u2014 enabling seamless integration with Google Cloud agent services like Agent Platform Runtime (Reasoning Engine), CX Agent Studio (Customer Engagement Suite), Conversational Agents (Dialogflow CX), and the Agent Development Kit (ADK).</p>"},{"location":"#features","title":"Features","text":""},{"location":"#agent-runtime-integration","title":"Agent Runtime Integration","text":"<ul> <li>GCP Agent Platform Runtime (Reasoning Engine) \u2014 two connectors, one per deployment type:</li> <li><code>ADKAgentEngineConnector</code> \u2014 Google ADK agents, session-based, streaming <code>async_stream_query()</code></li> <li><code>StudioAgentEngineConnector</code> \u2014 Agent Studio apps, invoked over the <code>streamQuery</code> REST endpoint</li> <li>CX Agent Studio (Customer Engagement Suite) via <code>CXAgentStudioConnector</code> \u2014 connect an agent built in the CX Agent Studio console; SMS/RCS/WhatsApp/Chat + ConversationRelay voice invoke the text <code>runSession</code> API, or switch voice to native speech-to-speech over <code>BidiRunSession</code></li> <li>Conversational Agents (Dialogflow CX) via <code>ConversationalAgentsConnector</code> \u2014 connect an agent built in the Conversational Agents console; invoked over the Dialogflow CX <code>detectIntent</code> API (works for Playbook and Flow agents)</li> </ul>"},{"location":"#multi-channel-communication","title":"Multi-Channel Communication","text":"<ul> <li>Voice, SMS, RCS, WhatsApp, and Chat support - Single codebase handles phone calls and every messaging channel; SMS/Voice/Chat are always available, RCS/WhatsApp activate automatically once their Twilio resource (<code>TWILIO_RCS_SENDER_ID</code> / <code>TWILIO_WHATSAPP_NUMBER</code>) is configured</li> <li>Automatic conversation routing - Messages route to the correct agent instance per conversation</li> <li>Memory injection - Customer history and preferences automatically included in agent context</li> </ul>"},{"location":"#deployment-options","title":"Deployment Options","text":"<ul> <li>Cloud Run server \u2b50 Recommended - Containerized TAC server with a public HTTPS URL and WebSocket support (required for Twilio ConversationRelay voice)</li> <li>Local (ngrok) - Run the FastAPI server locally against a deployed agent for testing</li> </ul>"},{"location":"#production-ready","title":"Production Ready","text":"<ul> <li>Twilio webhook validation - Automatic signature verification for secure integrations</li> <li>Secret Manager - Twilio credentials stored in Google Secret Manager, injected at runtime</li> <li>Session &amp; memory - Server-side sessions (ADK and Agent Studio), with Conversation Memory injection</li> </ul>"},{"location":"#installation","title":"Installation","text":""},{"location":"#with-agent-platform-runtime","title":"With Agent Platform Runtime","text":"<pre><code>pip install twilio-agent-connect-google[vertex-ai,server]\n</code></pre>"},{"location":"#with-cx-agent-studio","title":"With CX Agent Studio","text":"<pre><code>pip install twilio-agent-connect-google[cx-agent-studio,server]\n</code></pre>"},{"location":"#with-conversational-agents","title":"With Conversational Agents","text":"<pre><code>pip install twilio-agent-connect-google[conversational-agents,server]\n</code></pre>"},{"location":"#development","title":"Development","text":"<pre><code># Install with development tools (includes all connectors)\npip install twilio-agent-connect-google[dev]\n</code></pre> <p>Note: Requires Python 3.10 or 3.11. Python 3.12+ is not supported because GCP Agent Platform Runtime (Reasoning Engine) only supports Python 3.8\u20133.11. We recommend Python 3.11.</p>"},{"location":"#configuration","title":"Configuration","text":"<p>twilio-agent-connect-google requires TAC environment variables. See TAC Configuration for details.</p>"},{"location":"#required-environment-variables","title":"Required Environment Variables","text":"<pre><code># GCP Configuration\nGOOGLE_CLOUD_PROJECT=your-project-id\nGOOGLE_CLOUD_LOCATION=your-region\nGCP_REASONING_ENGINE_ID=your-reasoning-engine-id\n\n# Twilio Configuration\nTWILIO_ACCOUNT_SID=your_account_sid\nTWILIO_AUTH_TOKEN=your_auth_token\nTWILIO_API_KEY=your_api_key          # Starts with SK\nTWILIO_API_SECRET=your_api_secret    # Secret for API key\nTWILIO_PHONE_NUMBER=+1234567890\nTWILIO_CONVERSATION_CONFIGURATION_ID=conv_configuration_xxx\n\n# Optional: enables RCS / WhatsApp\n# TWILIO_RCS_SENDER_ID=rcs_sender_xxxxxxxxxxxxxxxxxx\n# TWILIO_WHATSAPP_NUMBER=whatsapp:+1234567890\n\n# Server Configuration (for Voice)\nTWILIO_VOICE_PUBLIC_DOMAIN=your-domain.ngrok.io\n</code></pre>"},{"location":"#examples","title":"Examples","text":"<p>Full examples available in <code>getting_started/examples/</code>:</p> <ul> <li><code>agent_platform/adk_agent_engine.py</code> - Connect Twilio to an ADK agent deployed on GCP Agent Platform Runtime</li> <li><code>agent_platform/studio_agent_engine.py</code> - Connect Twilio to an Agent Studio app deployed on GCP Agent Platform Runtime</li> <li><code>cx_agent_studio/cascaded.py</code> - Connect Twilio to an agent built in CX Agent Studio (Customer Engagement Suite), text/SMS + ConversationRelay voice</li> <li><code>cx_agent_studio/s2s.py</code> - Same CX Agent Studio agent, but native speech-to-speech voice (Twilio Media Streams + <code>BidiRunSession</code>, no ConversationRelay)</li> <li><code>conversational_agents.py</code> - Connect Twilio to an agent built in Conversational Agents (Dialogflow CX)</li> <li><code>features/whatsapp.py</code>, <code>features/rcs.py</code> - Enable a single messaging channel (WhatsApp or RCS) on its own</li> <li><code>features/chat/</code> - Browser-based web chat (Twilio Conversations JS SDK), runnable entirely on localhost</li> </ul>"},{"location":"#deployment","title":"Deployment","text":"<p>See <code>deploy/</code> for production deployment guides:</p>"},{"location":"#cloud-run-agent-platform-runtime-recommended","title":"Cloud Run (Agent Platform Runtime) \u2b50 Recommended","text":"<ul> <li>Deploy your agent (ADK, or a source-code app built in Agent Studio) to Agent Platform Runtime and the TAC server to Cloud Run</li> <li>Public HTTPS URL + WebSocket for Twilio ConversationRelay (voice)</li> <li>Twilio credentials stored in Secret Manager</li> <li>Two fully independent setups (<code>adk/</code> and <code>studio/</code>), each with its own <code>.env</code> and <code>make</code> workflow (<code>create-sa</code>, <code>deploy-secret</code>, <code>deploy-server</code>, <code>deploy-all</code>)</li> <li>See <code>deploy/agent_platform/</code> for the setup guide</li> </ul>"},{"location":"#cloud-run-cx-agent-studio","title":"Cloud Run (CX Agent Studio)","text":"<ul> <li>Build the agent in the CX Agent Studio console; deploy the TAC server to Cloud Run</li> <li>Same Cloud Run + Secret Manager + <code>make</code> workflow; voice defaults to ConversationRelay (<code>runSession</code>) but can switch to native speech-to-speech (<code>BidiRunSession</code>)</li> <li>See <code>deploy/cx_agent_studio/</code> for the setup guide</li> </ul>"},{"location":"#cloud-run-conversational-agents","title":"Cloud Run (Conversational Agents)","text":"<ul> <li>Build the agent in the Conversational Agents (Dialogflow CX) console; deploy the TAC server to Cloud Run</li> <li>Same Cloud Run + Secret Manager + <code>make</code> workflow, invoking the Dialogflow CX <code>detectIntent</code> API</li> <li>See <code>deploy/conversational_agents/</code> for the setup guide</li> </ul>"},{"location":"#development_1","title":"Development","text":""},{"location":"#setup","title":"Setup","text":"<pre><code># Clone repository\ngit clone https://github.com/twilio/twilio-agent-connect-google.git\ncd twilio-agent-connect-google\n\n# Install dependencies\nmake sync\n\n# Setup dev environment\nmake dev-setup\n</code></pre>"},{"location":"#code-quality","title":"Code Quality","text":"<pre><code># Format code\nmake format\n\n# Type check\nmake type-check\n\n# Lint\nmake lint\n\n# Run tests\nmake test\n\n# Run all checks\nmake check\n</code></pre>"},{"location":"#dependencies","title":"Dependencies","text":"<p>twilio-agent-connect-google depends on: - twilio-agent-connect - Core Twilio Agent Connect framework   - Requires the <code>[server]</code> extra for TAC Server support - google-cloud-aiplatform - Vertex AI / Agent Platform Runtime (Reasoning Engine) - google-adk (optional) - Google Agent Development Kit, for ADK agents - google-auth, requests, websockets - used by the CX Agent Studio connector to call the CX Agent Studio API (via the <code>cx-agent-studio</code> extra)</p>"},{"location":"#contributing","title":"Contributing","text":"<p>Contributions are welcome! Please feel free to submit a Pull Request.</p>"},{"location":"#license","title":"License","text":"<p>MIT License - see LICENSE file for details.</p>"},{"location":"api/agent_platform/","title":"Agent Platform Runtime","text":"<p>Connectors for agents deployed to GCP Agent Platform Runtime (Reasoning Engine). Requires the <code>vertex-ai</code> extra.</p>"},{"location":"api/agent_platform/#tac_google.connectors.agent_platform.adk_connector","title":"tac_google.connectors.agent_platform.adk_connector","text":"<p>Connector for ADK agents deployed on GCP Agent Platform Runtime (Agent Engine).</p>"},{"location":"api/agent_platform/#tac_google.connectors.agent_platform.adk_connector.ADKAgentEngineConnector","title":"ADKAgentEngineConnector","text":"<pre><code>ADKAgentEngineConnector(tac: TAC, agent: AgentEngine, sms_config: SMSChannelConfig | dict[str, Any] | None = None, voice_config: VoiceChannelConfig | dict[str, Any] | None = None, rcs_config: RCSChannelConfig | dict[str, Any] | None = None, whatsapp_config: WhatsAppChannelConfig | dict[str, Any] | None = None, chat_config: ChatChannelConfig | dict[str, Any] | None = None)\n</code></pre> <p>               Bases: <code>AgentEngineConnectorBase</code></p> <p>Connector for an ADK agent deployed on GCP Agent Platform Runtime (Agent Engine).</p> <p>ADK deployments are session-based and streaming: a session is created once per conversation (over REST, to get a clean \"already exists\" signal \u2014 see <code>AgentEngineConnectorBase._create_session</code>) and reused, with <code>async_stream_query()</code> invoked per message via the SDK \u2014 <code>async_stream_query</code> is always registered for an ADK deployment (ADK's own AdkApp template hard-codes this in its <code>register_operations()</code>), so no method-detection is needed for it.</p> <p>Conversation Memory is injected on every message, wrapped in <code>&lt;MEMORY&gt;...&lt;/MEMORY&gt;</code> ahead of the user's text (which is itself wrapped in <code>&lt;USER_MESSAGE&gt;...&lt;/USER_MESSAGE&gt;</code>), since ADK's <code>stream_query</code>/<code>async_stream_query</code> API has only one content channel (<code>message</code>) \u2014 there is no separate instructions/context parameter.</p> <p>Parameters:</p> Name Type Description Default <code>tac</code> <code>TAC</code> <p>TAC instance for channel integration</p> required <code>agent</code> <code>AgentEngine</code> <p>Deployed ADK agent instance, from <code>vertexai.Client().agent_engines.get(name=...)</code>.</p> required <code>sms_config</code> <code>SMSChannelConfig | dict[str, Any] | None</code> <p>SMS channel config; the channel is always built.</p> <code>None</code> <code>voice_config</code> <code>VoiceChannelConfig | dict[str, Any] | None</code> <p>Voice channel config; the channel is always built.</p> <code>None</code> <code>chat_config</code> <code>ChatChannelConfig | dict[str, Any] | None</code> <p>Chat channel config; the channel is always built.</p> <code>None</code> <code>rcs_config</code> <code>RCSChannelConfig | dict[str, Any] | None</code> <p>RCS channel config \u2014 tuning only. The channel is built whenever TWILIO_RCS_SENDER_ID is configured, regardless of this argument.</p> <code>None</code> <code>whatsapp_config</code> <code>WhatsAppChannelConfig | dict[str, Any] | None</code> <p>WhatsApp channel config \u2014 tuning only. The channel is built whenever TWILIO_WHATSAPP_NUMBER is configured, regardless of this argument.</p> <code>None</code> <p>Attributes:</p> Name Type Description <code>voice,</code> <code>(sms, chat)</code> <p>the corresponding channel instance.</p> <code>rcs,</code> <code>whatsapp</code> <p>the corresponding channel instance, or None if its Twilio resource isn't configured.</p> Example <pre><code>import vertexai\nfrom tac import TAC, TACConfig\nfrom tac.server import TACFastAPIServer\nfrom tac_google.connectors import ADKAgentEngineConnector\n\nclient = vertexai.Client(project=\"my-project\", location=\"us-central1\")\ntac = TAC(config=TACConfig.from_env())\n\nagent = client.agent_engines.get(\n    name=\"projects/my-project/locations/us-central1/reasoningEngines/123456\"\n)\n\nconnector = ADKAgentEngineConnector(tac=tac, agent=agent)\n\nserver = TACFastAPIServer(\n    tac=tac,\n    voice_channel=connector.voice,\n    messaging_channels=[connector.sms],\n)\nserver.start()\n</code></pre>"},{"location":"api/agent_platform/#tac_google.connectors.agent_platform.studio_connector","title":"tac_google.connectors.agent_platform.studio_connector","text":"<p>Connector for Agent Studio apps deployed on GCP Agent Platform Runtime (Agent Engine).</p>"},{"location":"api/agent_platform/#tac_google.connectors.agent_platform.studio_connector.StudioAgentEngineConnector","title":"StudioAgentEngineConnector","text":"<pre><code>StudioAgentEngineConnector(tac: TAC, agent: AgentEngine, sms_config: SMSChannelConfig | dict[str, Any] | None = None, voice_config: VoiceChannelConfig | dict[str, Any] | None = None, rcs_config: RCSChannelConfig | dict[str, Any] | None = None, whatsapp_config: WhatsAppChannelConfig | dict[str, Any] | None = None, chat_config: ChatChannelConfig | dict[str, Any] | None = None)\n</code></pre> <p>               Bases: <code>AgentEngineConnectorBase</code></p> <p>Connector for a source-code app deployed from the Agent Studio UI.</p> <p>Agent Studio apps are ADK apps under the hood (Studio's own \"Get Code\" feature generates a standard <code>google.adk</code> <code>LlmAgent</code>), but they aren't registered with <code>query</code>/<code>stream_query</code> methods the SDK can bind to (the SDK doesn't detect class methods for them at all), so this connector talks to them directly over REST instead of through SDK-bound methods: - Session creation via <code>AgentEngineConnectorBase._create_session</code> (same   Sessions REST API the ADK connector also uses directly, for the same   reason: a clean \"already exists\" signal). - <code>POST .../:streamQuery</code> with <code>class_method: \"async_stream_query\"</code> to   invoke it, passing only this turn's message and the <code>session_id</code> \u2014   same as the ADK connector, ADK reconstructs the full history from the   session's stored events on every call, so there's no local   conversation history to maintain here either.</p> <p>Conversation Memory is injected on every message, wrapped in <code>&lt;MEMORY&gt;...&lt;/MEMORY&gt;</code> ahead of the user's text (which is itself wrapped in <code>&lt;USER_MESSAGE&gt;...&lt;/USER_MESSAGE&gt;</code>), for the same reason as the ADK connector: there's no separate instructions/context parameter, only the one <code>message</code> channel.</p> <p>Parameters:</p> Name Type Description Default <code>tac</code> <code>TAC</code> <p>TAC instance for channel integration</p> required <code>agent</code> <code>AgentEngine</code> <p>Deployed Studio agent instance, from <code>vertexai.Client().agent_engines.get(name=...)</code>.</p> required <code>sms_config</code> <code>SMSChannelConfig | dict[str, Any] | None</code> <p>SMS channel config; the channel is always built.</p> <code>None</code> <code>voice_config</code> <code>VoiceChannelConfig | dict[str, Any] | None</code> <p>Voice channel config; the channel is always built.</p> <code>None</code> <code>chat_config</code> <code>ChatChannelConfig | dict[str, Any] | None</code> <p>Chat channel config; the channel is always built.</p> <code>None</code> <code>rcs_config</code> <code>RCSChannelConfig | dict[str, Any] | None</code> <p>RCS channel config \u2014 tuning only. The channel is built whenever TWILIO_RCS_SENDER_ID is configured, regardless of this argument.</p> <code>None</code> <code>whatsapp_config</code> <code>WhatsAppChannelConfig | dict[str, Any] | None</code> <p>WhatsApp channel config \u2014 tuning only. The channel is built whenever TWILIO_WHATSAPP_NUMBER is configured, regardless of this argument.</p> <code>None</code> <p>Attributes:</p> Name Type Description <code>voice,</code> <code>(sms, chat)</code> <p>the corresponding channel instance.</p> <code>rcs,</code> <code>whatsapp</code> <p>the corresponding channel instance, or None if its Twilio resource isn't configured.</p> Example <pre><code>import vertexai\nfrom tac import TAC, TACConfig\nfrom tac.server import TACFastAPIServer\nfrom tac_google.connectors import StudioAgentEngineConnector\n\nclient = vertexai.Client(project=\"my-project\", location=\"us-central1\")\ntac = TAC(config=TACConfig.from_env())\n\nagent = client.agent_engines.get(\n    name=\"projects/my-project/locations/us-central1/reasoningEngines/123456\"\n)\n\nconnector = StudioAgentEngineConnector(tac=tac, agent=agent)\n\nserver = TACFastAPIServer(\n    tac=tac,\n    voice_channel=connector.voice,\n    messaging_channels=[connector.sms],\n)\nserver.start()\n</code></pre>"},{"location":"api/connectors/","title":"Connectors","text":"<p>Connectors combine a Google Cloud agent runtime integration with TAC channel management. Import them from <code>tac_google.connectors</code> \u2014 the top-level <code>tac_google</code> package deliberately exposes only <code>__version__</code>, so installing without extras never fails on import.</p>"},{"location":"api/connectors/#tac_google.connectors","title":"tac_google.connectors","text":"<p>TAC connectors for GCP agent runtimes.</p>"},{"location":"api/connectors/#tac_google.connectors.ADKAgentEngineConnector","title":"ADKAgentEngineConnector","text":"<pre><code>ADKAgentEngineConnector(tac: TAC, agent: AgentEngine, sms_config: SMSChannelConfig | dict[str, Any] | None = None, voice_config: VoiceChannelConfig | dict[str, Any] | None = None, rcs_config: RCSChannelConfig | dict[str, Any] | None = None, whatsapp_config: WhatsAppChannelConfig | dict[str, Any] | None = None, chat_config: ChatChannelConfig | dict[str, Any] | None = None)\n</code></pre> <p>               Bases: <code>AgentEngineConnectorBase</code></p> <p>Connector for an ADK agent deployed on GCP Agent Platform Runtime (Agent Engine).</p> <p>ADK deployments are session-based and streaming: a session is created once per conversation (over REST, to get a clean \"already exists\" signal \u2014 see <code>AgentEngineConnectorBase._create_session</code>) and reused, with <code>async_stream_query()</code> invoked per message via the SDK \u2014 <code>async_stream_query</code> is always registered for an ADK deployment (ADK's own AdkApp template hard-codes this in its <code>register_operations()</code>), so no method-detection is needed for it.</p> <p>Conversation Memory is injected on every message, wrapped in <code>&lt;MEMORY&gt;...&lt;/MEMORY&gt;</code> ahead of the user's text (which is itself wrapped in <code>&lt;USER_MESSAGE&gt;...&lt;/USER_MESSAGE&gt;</code>), since ADK's <code>stream_query</code>/<code>async_stream_query</code> API has only one content channel (<code>message</code>) \u2014 there is no separate instructions/context parameter.</p> <p>Parameters:</p> Name Type Description Default <code>tac</code> <code>TAC</code> <p>TAC instance for channel integration</p> required <code>agent</code> <code>AgentEngine</code> <p>Deployed ADK agent instance, from <code>vertexai.Client().agent_engines.get(name=...)</code>.</p> required <code>sms_config</code> <code>SMSChannelConfig | dict[str, Any] | None</code> <p>SMS channel config; the channel is always built.</p> <code>None</code> <code>voice_config</code> <code>VoiceChannelConfig | dict[str, Any] | None</code> <p>Voice channel config; the channel is always built.</p> <code>None</code> <code>chat_config</code> <code>ChatChannelConfig | dict[str, Any] | None</code> <p>Chat channel config; the channel is always built.</p> <code>None</code> <code>rcs_config</code> <code>RCSChannelConfig | dict[str, Any] | None</code> <p>RCS channel config \u2014 tuning only. The channel is built whenever TWILIO_RCS_SENDER_ID is configured, regardless of this argument.</p> <code>None</code> <code>whatsapp_config</code> <code>WhatsAppChannelConfig | dict[str, Any] | None</code> <p>WhatsApp channel config \u2014 tuning only. The channel is built whenever TWILIO_WHATSAPP_NUMBER is configured, regardless of this argument.</p> <code>None</code> <p>Attributes:</p> Name Type Description <code>voice,</code> <code>(sms, chat)</code> <p>the corresponding channel instance.</p> <code>rcs,</code> <code>whatsapp</code> <p>the corresponding channel instance, or None if its Twilio resource isn't configured.</p> Example <pre><code>import vertexai\nfrom tac import TAC, TACConfig\nfrom tac.server import TACFastAPIServer\nfrom tac_google.connectors import ADKAgentEngineConnector\n\nclient = vertexai.Client(project=\"my-project\", location=\"us-central1\")\ntac = TAC(config=TACConfig.from_env())\n\nagent = client.agent_engines.get(\n    name=\"projects/my-project/locations/us-central1/reasoningEngines/123456\"\n)\n\nconnector = ADKAgentEngineConnector(tac=tac, agent=agent)\n\nserver = TACFastAPIServer(\n    tac=tac,\n    voice_channel=connector.voice,\n    messaging_channels=[connector.sms],\n)\nserver.start()\n</code></pre>"},{"location":"api/connectors/#tac_google.connectors.StudioAgentEngineConnector","title":"StudioAgentEngineConnector","text":"<pre><code>StudioAgentEngineConnector(tac: TAC, agent: AgentEngine, sms_config: SMSChannelConfig | dict[str, Any] | None = None, voice_config: VoiceChannelConfig | dict[str, Any] | None = None, rcs_config: RCSChannelConfig | dict[str, Any] | None = None, whatsapp_config: WhatsAppChannelConfig | dict[str, Any] | None = None, chat_config: ChatChannelConfig | dict[str, Any] | None = None)\n</code></pre> <p>               Bases: <code>AgentEngineConnectorBase</code></p> <p>Connector for a source-code app deployed from the Agent Studio UI.</p> <p>Agent Studio apps are ADK apps under the hood (Studio's own \"Get Code\" feature generates a standard <code>google.adk</code> <code>LlmAgent</code>), but they aren't registered with <code>query</code>/<code>stream_query</code> methods the SDK can bind to (the SDK doesn't detect class methods for them at all), so this connector talks to them directly over REST instead of through SDK-bound methods: - Session creation via <code>AgentEngineConnectorBase._create_session</code> (same   Sessions REST API the ADK connector also uses directly, for the same   reason: a clean \"already exists\" signal). - <code>POST .../:streamQuery</code> with <code>class_method: \"async_stream_query\"</code> to   invoke it, passing only this turn's message and the <code>session_id</code> \u2014   same as the ADK connector, ADK reconstructs the full history from the   session's stored events on every call, so there's no local   conversation history to maintain here either.</p> <p>Conversation Memory is injected on every message, wrapped in <code>&lt;MEMORY&gt;...&lt;/MEMORY&gt;</code> ahead of the user's text (which is itself wrapped in <code>&lt;USER_MESSAGE&gt;...&lt;/USER_MESSAGE&gt;</code>), for the same reason as the ADK connector: there's no separate instructions/context parameter, only the one <code>message</code> channel.</p> <p>Parameters:</p> Name Type Description Default <code>tac</code> <code>TAC</code> <p>TAC instance for channel integration</p> required <code>agent</code> <code>AgentEngine</code> <p>Deployed Studio agent instance, from <code>vertexai.Client().agent_engines.get(name=...)</code>.</p> required <code>sms_config</code> <code>SMSChannelConfig | dict[str, Any] | None</code> <p>SMS channel config; the channel is always built.</p> <code>None</code> <code>voice_config</code> <code>VoiceChannelConfig | dict[str, Any] | None</code> <p>Voice channel config; the channel is always built.</p> <code>None</code> <code>chat_config</code> <code>ChatChannelConfig | dict[str, Any] | None</code> <p>Chat channel config; the channel is always built.</p> <code>None</code> <code>rcs_config</code> <code>RCSChannelConfig | dict[str, Any] | None</code> <p>RCS channel config \u2014 tuning only. The channel is built whenever TWILIO_RCS_SENDER_ID is configured, regardless of this argument.</p> <code>None</code> <code>whatsapp_config</code> <code>WhatsAppChannelConfig | dict[str, Any] | None</code> <p>WhatsApp channel config \u2014 tuning only. The channel is built whenever TWILIO_WHATSAPP_NUMBER is configured, regardless of this argument.</p> <code>None</code> <p>Attributes:</p> Name Type Description <code>voice,</code> <code>(sms, chat)</code> <p>the corresponding channel instance.</p> <code>rcs,</code> <code>whatsapp</code> <p>the corresponding channel instance, or None if its Twilio resource isn't configured.</p> Example <pre><code>import vertexai\nfrom tac import TAC, TACConfig\nfrom tac.server import TACFastAPIServer\nfrom tac_google.connectors import StudioAgentEngineConnector\n\nclient = vertexai.Client(project=\"my-project\", location=\"us-central1\")\ntac = TAC(config=TACConfig.from_env())\n\nagent = client.agent_engines.get(\n    name=\"projects/my-project/locations/us-central1/reasoningEngines/123456\"\n)\n\nconnector = StudioAgentEngineConnector(tac=tac, agent=agent)\n\nserver = TACFastAPIServer(\n    tac=tac,\n    voice_channel=connector.voice,\n    messaging_channels=[connector.sms],\n)\nserver.start()\n</code></pre>"},{"location":"api/connectors/#tac_google.connectors.ConversationalAgentsConnector","title":"ConversationalAgentsConnector","text":"<pre><code>ConversationalAgentsConnector(tac: TAC, agent_id: str, language_code: str = 'en', sms_config: SMSChannelConfig | dict[str, Any] | None = None, voice_config: VoiceChannelConfig | dict[str, Any] | None = None, rcs_config: RCSChannelConfig | dict[str, Any] | None = None, whatsapp_config: WhatsAppChannelConfig | dict[str, Any] | None = None, chat_config: ChatChannelConfig | dict[str, Any] | None = None)\n</code></pre> <p>Connector for agents built in Conversational Agents (Dialogflow CX).</p> <p>Dialogflow CX agents are invoked over the <code>detectIntent</code> REST API (v3):</p> <pre><code>POST https://&lt;location&gt;-dialogflow.googleapis.com/v3/&lt;agent&gt;/sessions/&lt;session&gt;:detectIntent\nbody: {\"queryInput\": {\"text\": {\"text\": \"&lt;message&gt;\"}, \"languageCode\": \"en\"}}\nreply: response[\"queryResult\"][\"responseMessages\"][*][\"text\"][\"text\"]\n</code></pre> <p>Works for both Playbook- and Flow-based agents \u2014 the runtime API is the same. Voice runs on Twilio ConversationRelay (text), so only text is exchanged; SMS is text too.</p> <p>Conversation history is kept server-side by Dialogflow, keyed by session id (default 30 min). This connector derives one stable session id per conversation (a UUID hash of the TAC conversation id, to fit Dialogflow's 36-char session id limit) and reuses it on every turn, so Dialogflow keeps the context (no local history is built). Conversation Memory is injected only when it changes turn-to-turn (see <code>_maybe_tag_memory</code>).</p> <p>Parameters:</p> Name Type Description Default <code>tac</code> <code>TAC</code> <p>TAC instance for channel integration.</p> required <code>agent_id</code> <code>str</code> <p>The Dialogflow CX agent resource name, e.g. <code>projects/&lt;project&gt;/locations/&lt;location&gt;/agents/&lt;agent-id&gt;</code>.</p> required <code>language_code</code> <code>str</code> <p>Language code for queries (default \"en\").</p> <code>'en'</code> <code>sms_config</code> <code>SMSChannelConfig | dict[str, Any] | None</code> <p>SMS channel config; the channel is always built.</p> <code>None</code> <code>voice_config</code> <code>VoiceChannelConfig | dict[str, Any] | None</code> <p>Voice channel config; the channel is always built.</p> <code>None</code> <code>chat_config</code> <code>ChatChannelConfig | dict[str, Any] | None</code> <p>Chat channel config; the channel is always built.</p> <code>None</code> <code>rcs_config</code> <code>RCSChannelConfig | dict[str, Any] | None</code> <p>RCS channel config \u2014 tuning only. The channel is built whenever TWILIO_RCS_SENDER_ID is configured, regardless of this argument.</p> <code>None</code> <code>whatsapp_config</code> <code>WhatsAppChannelConfig | dict[str, Any] | None</code> <p>WhatsApp channel config \u2014 tuning only. The channel is built whenever TWILIO_WHATSAPP_NUMBER is configured, regardless of this argument.</p> <code>None</code> <p>Attributes:</p> Name Type Description <code>voice,</code> <code>(sms, chat)</code> <p>the corresponding channel instance.</p> <code>rcs,</code> <code>whatsapp</code> <p>the corresponding channel instance, or None if its Twilio resource isn't configured.</p>"},{"location":"api/connectors/#tac_google.connectors.CXAgentStudioConnector","title":"CXAgentStudioConnector","text":"<pre><code>CXAgentStudioConnector(tac: TAC, agent_id: str, sms_config: SMSChannelConfig | dict[str, Any] | None = None, voice_config: VoiceChannelConfig | VoiceS2SConfig | None = None, rcs_config: RCSChannelConfig | dict[str, Any] | None = None, whatsapp_config: WhatsAppChannelConfig | dict[str, Any] | None = None, chat_config: ChatChannelConfig | dict[str, Any] | None = None)\n</code></pre> <p>Connector for agents built in CX Agent Studio (Customer Engagement Suite).</p> <p>CX Agent Studio agents aren't Agent Platform Runtime (reasoningEngines) resources and expose no query/stream_query SDK methods, so this connector calls the CES REST API's text <code>runSession</code> method directly (v1):</p> <pre><code>POST https://ces.googleapis.com/v1/&lt;app&gt;/sessions/&lt;session&gt;:runSession\nbody: {\"config\": {\"session\": \"&lt;app&gt;/sessions/&lt;session&gt;\"},\n       \"inputs\": [{\"text\": \"&lt;message&gt;\"}]}\nreply: response[\"outputs\"][*][\"text\"]\n</code></pre> <p>Text is all this connector exchanges with the agent, for both SMS and (ConversationRelay) voice \u2014 the CES native-audio streaming session isn't used here.</p> <p>Conversation history is kept server-side by CES: the TAC conversation id doubles as the CES session id, so every turn lands in the same session and no local history is built. Conversation Memory is injected only when it changes turn-to-turn (see <code>_maybe_tag_memory</code>).</p> <p>Every CX Agent Studio agent has an <code>end_session</code> tool attached by default, letting the model decide on its own when a call is done \u2014 see <code>_handle_end_session</code>.</p> <p>Parameters:</p> Name Type Description Default <code>tac</code> <code>TAC</code> <p>TAC instance for channel integration.</p> required <code>agent_id</code> <code>str</code> <p>The CES agent (app) resource name, e.g. <code>projects/&lt;project&gt;/locations/&lt;location&gt;/apps/&lt;app-id&gt;</code>.</p> required <code>sms_config</code> <code>SMSChannelConfig | dict[str, Any] | None</code> <p>SMS channel config; the channel is always built.</p> <code>None</code> <code>chat_config</code> <code>ChatChannelConfig | dict[str, Any] | None</code> <p>Chat channel config; the channel is always built.</p> <code>None</code> <code>rcs_config</code> <code>RCSChannelConfig | dict[str, Any] | None</code> <p>RCS channel config \u2014 tuning only. The channel is built whenever TWILIO_RCS_SENDER_ID is configured, regardless of this argument.</p> <code>None</code> <code>whatsapp_config</code> <code>WhatsAppChannelConfig | dict[str, Any] | None</code> <p>WhatsApp channel config \u2014 tuning only. The channel is built whenever TWILIO_WHATSAPP_NUMBER is configured, regardless of this argument.</p> <code>None</code> <code>voice_config</code> <code>VoiceChannelConfig | VoiceS2SConfig | None</code> <p>A <code>VoiceChannelConfig</code> builds <code>self.voice_cascaded</code> (ConversationRelay voice); a <code>VoiceS2SConfig</code> builds <code>self.voice_s2s</code> (native speech-to-speech) \u2014 the type decides which, so a plain dict isn't accepted. None (default) for no voice channel.</p> <code>None</code> <p>Attributes:</p> Name Type Description <code>sms,</code> <code>chat</code> <p>the corresponding channel instance.</p> <code>rcs,</code> <code>whatsapp</code> <p>the corresponding channel instance, or None if its Twilio resource isn't configured.</p> <code>voice_cascaded</code> <code>VoiceChannel | None</code> <p>VoiceChannel for ConversationRelay voice, or None.</p> <code>voice_s2s</code> <code>VoiceS2SChannel | None</code> <p>VoiceS2SChannel for native speech-to-speech voice, or None.</p> <code>voice</code> <code>VoiceChannel | VoiceS2SChannel | None</code> <p>Whichever of the two above actually got built, or None \u2014 for callers that don't care which voice approach is active, e.g. a server wiring up <code>voice_channel=connector.voice</code>.</p>"},{"location":"api/connectors/#tac_google.connectors.VoiceS2SChannel","title":"VoiceS2SChannel","text":"<pre><code>VoiceS2SChannel(tac: TAC, agent_id: str, config: VoiceS2SConfig | dict[str, Any] | None = None)\n</code></pre> <p>               Bases: <code>BaseChannel</code></p> <p>Native speech-to-speech voice channel for a CX Agent Studio (CES) agent.</p> <p>Parameters:</p> Name Type Description Default <code>tac</code> <code>TAC</code> <p>TAC instance (used only for conversation lifecycle bookkeeping \u2014 see module docstring).</p> required <code>agent_id</code> <code>str</code> <p>The CES agent (app) resource name, e.g. <code>projects/&lt;project&gt;/locations/&lt;location&gt;/apps/&lt;app-id&gt;</code>.</p> required <code>config</code> <code>VoiceS2SConfig | dict[str, Any] | None</code> <p>Voice S2S channel configuration (VoiceS2SConfig or dict). If None, uses default configuration (no deployment_id).</p> <code>None</code>"},{"location":"api/connectors/#tac_google.connectors.VoiceS2SChannel.handle_incoming_call","title":"handle_incoming_call  <code>async</code>","text":"<pre><code>handle_incoming_call(websocket_url: str | None = None) -&gt; str\n</code></pre> <p>Generate TwiML that connects the call to Twilio Media Streams.</p> <p>Unlike ConversationRelay, this carries raw audio \u2014 CES does its own speech recognition/synthesis over <code>BidiRunSession</code>.</p> <p>TODO: only <code>websocket_url</code> is configurable right now. We'll likely want to expose more <code>&lt;Stream&gt;</code>/<code>&lt;Connect&gt;</code> TwiML options later (e.g. <code>name</code>, <code>status_callback</code>, custom <code>&lt;Parameter&gt;</code>s) \u2014 see <code>VoiceChannel</code>'s layered <code>TwiMLOptions</code> for the pattern to follow.</p> <p>Parameters:</p> Name Type Description Default <code>websocket_url</code> <code>str | None</code> <p>Per-call override for the WebSocket URL, supplied by the host (the code owning the route \u2014 e.g. a custom server) for transport facts this channel can't derive, such as a per-call affinity token. Takes precedence over the <code>TACConfig.voice_public_domain</code>-derived default.</p> <code>None</code>"},{"location":"api/connectors/#tac_google.connectors.VoiceS2SChannel.handle_websocket","title":"handle_websocket  <code>async</code>","text":"<pre><code>handle_websocket(websocket: WebSocketProtocol) -&gt; None\n</code></pre> <p>Bridge one call's Twilio Media Streams connection to a CES <code>BidiRunSession</code> websocket for the lifetime of the call.</p> <p>Opens the CES connection once Twilio's <code>start</code> event arrives (the CES session id is derived from the Twilio call sid), then runs Twilio\u2192CES and CES\u2192Twilio concurrently until either side ends the call \u2014 the first to finish cancels the other.</p>"},{"location":"api/connectors/#tac_google.connectors.VoiceS2SChannel.process_webhook","title":"process_webhook  <code>async</code>","text":"<pre><code>process_webhook(webhook_data: dict[str, Any], idempotency_token: str | None = None) -&gt; None\n</code></pre> <p>Not used \u2014 S2S has no Conversation Orchestrator text-turn webhook.</p>"},{"location":"api/connectors/#tac_google.connectors.VoiceS2SChannel.send_response","title":"send_response  <code>async</code>","text":"<pre><code>send_response(conversation_id: str, response: Any, role: str | None = None) -&gt; None\n</code></pre> <p>Not used \u2014 audio is forwarded directly inside <code>handle_websocket</code>, there's no discrete turn to push a response for.</p>"},{"location":"api/connectors/#tac_google.connectors.VoiceS2SConfig","title":"VoiceS2SConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Configuration for <code>VoiceS2SChannel</code>.</p> <p>Attributes:</p> Name Type Description <code>deployment_id</code> <code>str | None</code> <p>Optional CES deployment resource name, for agents with a dedicated Twilio channel deployment in the CES console.</p> <code>welcome_greeting</code> <code>str | None</code> <p>Text sent to CES as the session-kickoff turn. Unlike ConversationRelay's <code>welcome_greeting</code>, this isn't spoken verbatim \u2014 the agent decides how to respond to it, like any other turn. Set to <code>None</code> to let the agent's own configured greeting play unmodified.</p>"},{"location":"api/conversational_agents/","title":"Conversational Agents","text":"<p>Connector for agents built in Conversational Agents (Dialogflow CX), invoked over the <code>detectIntent</code> API. Requires the <code>conversational-agents</code> extra.</p>"},{"location":"api/conversational_agents/#tac_google.connectors.conversational_agents_connector","title":"tac_google.connectors.conversational_agents_connector","text":"<p>TAC connector for agents built in Conversational Agents (Dialogflow CX).</p>"},{"location":"api/conversational_agents/#tac_google.connectors.conversational_agents_connector.ConversationalAgentsConnector","title":"ConversationalAgentsConnector","text":"<pre><code>ConversationalAgentsConnector(tac: TAC, agent_id: str, language_code: str = 'en', sms_config: SMSChannelConfig | dict[str, Any] | None = None, voice_config: VoiceChannelConfig | dict[str, Any] | None = None, rcs_config: RCSChannelConfig | dict[str, Any] | None = None, whatsapp_config: WhatsAppChannelConfig | dict[str, Any] | None = None, chat_config: ChatChannelConfig | dict[str, Any] | None = None)\n</code></pre> <p>Connector for agents built in Conversational Agents (Dialogflow CX).</p> <p>Dialogflow CX agents are invoked over the <code>detectIntent</code> REST API (v3):</p> <pre><code>POST https://&lt;location&gt;-dialogflow.googleapis.com/v3/&lt;agent&gt;/sessions/&lt;session&gt;:detectIntent\nbody: {\"queryInput\": {\"text\": {\"text\": \"&lt;message&gt;\"}, \"languageCode\": \"en\"}}\nreply: response[\"queryResult\"][\"responseMessages\"][*][\"text\"][\"text\"]\n</code></pre> <p>Works for both Playbook- and Flow-based agents \u2014 the runtime API is the same. Voice runs on Twilio ConversationRelay (text), so only text is exchanged; SMS is text too.</p> <p>Conversation history is kept server-side by Dialogflow, keyed by session id (default 30 min). This connector derives one stable session id per conversation (a UUID hash of the TAC conversation id, to fit Dialogflow's 36-char session id limit) and reuses it on every turn, so Dialogflow keeps the context (no local history is built). Conversation Memory is injected only when it changes turn-to-turn (see <code>_maybe_tag_memory</code>).</p> <p>Parameters:</p> Name Type Description Default <code>tac</code> <code>TAC</code> <p>TAC instance for channel integration.</p> required <code>agent_id</code> <code>str</code> <p>The Dialogflow CX agent resource name, e.g. <code>projects/&lt;project&gt;/locations/&lt;location&gt;/agents/&lt;agent-id&gt;</code>.</p> required <code>language_code</code> <code>str</code> <p>Language code for queries (default \"en\").</p> <code>'en'</code> <code>sms_config</code> <code>SMSChannelConfig | dict[str, Any] | None</code> <p>SMS channel config; the channel is always built.</p> <code>None</code> <code>voice_config</code> <code>VoiceChannelConfig | dict[str, Any] | None</code> <p>Voice channel config; the channel is always built.</p> <code>None</code> <code>chat_config</code> <code>ChatChannelConfig | dict[str, Any] | None</code> <p>Chat channel config; the channel is always built.</p> <code>None</code> <code>rcs_config</code> <code>RCSChannelConfig | dict[str, Any] | None</code> <p>RCS channel config \u2014 tuning only. The channel is built whenever TWILIO_RCS_SENDER_ID is configured, regardless of this argument.</p> <code>None</code> <code>whatsapp_config</code> <code>WhatsAppChannelConfig | dict[str, Any] | None</code> <p>WhatsApp channel config \u2014 tuning only. The channel is built whenever TWILIO_WHATSAPP_NUMBER is configured, regardless of this argument.</p> <code>None</code> <p>Attributes:</p> Name Type Description <code>voice,</code> <code>(sms, chat)</code> <p>the corresponding channel instance.</p> <code>rcs,</code> <code>whatsapp</code> <p>the corresponding channel instance, or None if its Twilio resource isn't configured.</p>"},{"location":"api/cx_agent_studio/","title":"CX Agent Studio","text":"<p>Connector, FastAPI server and native speech-to-speech voice channel for agents built in CX Agent Studio (Customer Engagement Suite). Requires the <code>cx-agent-studio</code> extra.</p>"},{"location":"api/cx_agent_studio/#tac_google.connectors.cx_agent_studio.connector","title":"tac_google.connectors.cx_agent_studio.connector","text":"<p>TAC connector for agents built in CX Agent Studio (Customer Engagement Suite).</p>"},{"location":"api/cx_agent_studio/#tac_google.connectors.cx_agent_studio.connector.CXAgentStudioConnector","title":"CXAgentStudioConnector","text":"<pre><code>CXAgentStudioConnector(tac: TAC, agent_id: str, sms_config: SMSChannelConfig | dict[str, Any] | None = None, voice_config: VoiceChannelConfig | VoiceS2SConfig | None = None, rcs_config: RCSChannelConfig | dict[str, Any] | None = None, whatsapp_config: WhatsAppChannelConfig | dict[str, Any] | None = None, chat_config: ChatChannelConfig | dict[str, Any] | None = None)\n</code></pre> <p>Connector for agents built in CX Agent Studio (Customer Engagement Suite).</p> <p>CX Agent Studio agents aren't Agent Platform Runtime (reasoningEngines) resources and expose no query/stream_query SDK methods, so this connector calls the CES REST API's text <code>runSession</code> method directly (v1):</p> <pre><code>POST https://ces.googleapis.com/v1/&lt;app&gt;/sessions/&lt;session&gt;:runSession\nbody: {\"config\": {\"session\": \"&lt;app&gt;/sessions/&lt;session&gt;\"},\n       \"inputs\": [{\"text\": \"&lt;message&gt;\"}]}\nreply: response[\"outputs\"][*][\"text\"]\n</code></pre> <p>Text is all this connector exchanges with the agent, for both SMS and (ConversationRelay) voice \u2014 the CES native-audio streaming session isn't used here.</p> <p>Conversation history is kept server-side by CES: the TAC conversation id doubles as the CES session id, so every turn lands in the same session and no local history is built. Conversation Memory is injected only when it changes turn-to-turn (see <code>_maybe_tag_memory</code>).</p> <p>Every CX Agent Studio agent has an <code>end_session</code> tool attached by default, letting the model decide on its own when a call is done \u2014 see <code>_handle_end_session</code>.</p> <p>Parameters:</p> Name Type Description Default <code>tac</code> <code>TAC</code> <p>TAC instance for channel integration.</p> required <code>agent_id</code> <code>str</code> <p>The CES agent (app) resource name, e.g. <code>projects/&lt;project&gt;/locations/&lt;location&gt;/apps/&lt;app-id&gt;</code>.</p> required <code>sms_config</code> <code>SMSChannelConfig | dict[str, Any] | None</code> <p>SMS channel config; the channel is always built.</p> <code>None</code> <code>chat_config</code> <code>ChatChannelConfig | dict[str, Any] | None</code> <p>Chat channel config; the channel is always built.</p> <code>None</code> <code>rcs_config</code> <code>RCSChannelConfig | dict[str, Any] | None</code> <p>RCS channel config \u2014 tuning only. The channel is built whenever TWILIO_RCS_SENDER_ID is configured, regardless of this argument.</p> <code>None</code> <code>whatsapp_config</code> <code>WhatsAppChannelConfig | dict[str, Any] | None</code> <p>WhatsApp channel config \u2014 tuning only. The channel is built whenever TWILIO_WHATSAPP_NUMBER is configured, regardless of this argument.</p> <code>None</code> <code>voice_config</code> <code>VoiceChannelConfig | VoiceS2SConfig | None</code> <p>A <code>VoiceChannelConfig</code> builds <code>self.voice_cascaded</code> (ConversationRelay voice); a <code>VoiceS2SConfig</code> builds <code>self.voice_s2s</code> (native speech-to-speech) \u2014 the type decides which, so a plain dict isn't accepted. None (default) for no voice channel.</p> <code>None</code> <p>Attributes:</p> Name Type Description <code>sms,</code> <code>chat</code> <p>the corresponding channel instance.</p> <code>rcs,</code> <code>whatsapp</code> <p>the corresponding channel instance, or None if its Twilio resource isn't configured.</p> <code>voice_cascaded</code> <code>VoiceChannel | None</code> <p>VoiceChannel for ConversationRelay voice, or None.</p> <code>voice_s2s</code> <code>VoiceS2SChannel | None</code> <p>VoiceS2SChannel for native speech-to-speech voice, or None.</p> <code>voice</code> <code>VoiceChannel | VoiceS2SChannel | None</code> <p>Whichever of the two above actually got built, or None \u2014 for callers that don't care which voice approach is active, e.g. a server wiring up <code>voice_channel=connector.voice</code>.</p>"},{"location":"api/cx_agent_studio/#server","title":"Server","text":""},{"location":"api/cx_agent_studio/#tac_google.connectors.cx_agent_studio.server","title":"tac_google.connectors.cx_agent_studio.server","text":"<p>FastAPI server for CX Agent Studio voice \u2014 ConversationRelay or S2S.</p> <p><code>TACFastAPIServer.voice_channel</code> only accepts <code>VoiceChannel</code>, and <code>VoiceS2SChannel</code> isn't one (it just exposes the same <code>handle_incoming_call</code>/<code>handle_websocket</code> shape). This subclass accepts either type and dispatches on <code>isinstance</code>: a <code>VoiceChannel</code> goes to <code>TACFastAPIServer</code> as usual; a <code>VoiceS2SChannel</code> gets its own routes registered here instead.</p>"},{"location":"api/cx_agent_studio/#tac_google.connectors.cx_agent_studio.server.CXAgentStudioFastAPIServer","title":"CXAgentStudioFastAPIServer","text":"<pre><code>CXAgentStudioFastAPIServer(tac: TAC, voice_channel: VoiceChannel | VoiceS2SChannel | None = None, messaging_channels: list[MessagingChannel] | None = None, config: TACServerConfig | None = None, app: FastAPI | None = None)\n</code></pre> <p>               Bases: <code>TACFastAPIServer</code></p> <p><code>TACFastAPIServer</code> plus support for native speech-to-speech voice.</p> <p>Parameters:</p> Name Type Description Default <code>tac</code> <code>TAC</code> <p>TAC instance.</p> required <code>voice_channel</code> <code>VoiceChannel | VoiceS2SChannel | None</code> <p>Pass a <code>VoiceChannel</code> for ConversationRelay voice, a <code>VoiceS2SChannel</code> for native speech-to-speech voice, or None for no voice channel at all.</p> <code>None</code> <code>messaging_channels</code> <code>list[MessagingChannel] | None</code> <p>Messaging channels (SMS, etc.), e.g. <code>[connector.sms, connector.chat]</code>.</p> <code>None</code> <code>config</code> <code>TACServerConfig | None</code> <p><code>TACServerConfig</code>, same as <code>TACFastAPIServer</code>.</p> <code>None</code> <code>app</code> <code>FastAPI | None</code> <p>Existing <code>FastAPI</code> app to register routes onto, same as <code>TACFastAPIServer</code>.</p> <code>None</code>"},{"location":"api/cx_agent_studio/#native-speech-to-speech-voice","title":"Native speech-to-speech voice","text":""},{"location":"api/cx_agent_studio/#tac_google.connectors.cx_agent_studio.voice_s2s.channel","title":"tac_google.connectors.cx_agent_studio.voice_s2s.channel","text":"<p>Native speech-to-speech (S2S) voice channel for CX Agent Studio (CES).</p> <p>Bridges raw call audio between Twilio Media Streams and CES's <code>BidiRunSession</code>. CES does its own speech recognition/synthesis, and audio flows continuously with no discrete text turns \u2014 unlike <code>VoiceChannel</code> (Twilio ConversationRelay), where Twilio does the STT/TTS.</p> <p>Since there's no discrete turn, TAC's turn callbacks don't apply here; <code>process_webhook</code>/<code>send_response</code> are no-ops just to satisfy <code>BaseChannel</code>'s abstract interface.</p> <p>Supports neither Conversation Orchestrator nor Conversation Memory for voice, unlike <code>VoiceChannel</code> which supports both.</p>"},{"location":"api/cx_agent_studio/#tac_google.connectors.cx_agent_studio.voice_s2s.channel.VoiceS2SChannel","title":"VoiceS2SChannel","text":"<pre><code>VoiceS2SChannel(tac: TAC, agent_id: str, config: VoiceS2SConfig | dict[str, Any] | None = None)\n</code></pre> <p>               Bases: <code>BaseChannel</code></p> <p>Native speech-to-speech voice channel for a CX Agent Studio (CES) agent.</p> <p>Parameters:</p> Name Type Description Default <code>tac</code> <code>TAC</code> <p>TAC instance (used only for conversation lifecycle bookkeeping \u2014 see module docstring).</p> required <code>agent_id</code> <code>str</code> <p>The CES agent (app) resource name, e.g. <code>projects/&lt;project&gt;/locations/&lt;location&gt;/apps/&lt;app-id&gt;</code>.</p> required <code>config</code> <code>VoiceS2SConfig | dict[str, Any] | None</code> <p>Voice S2S channel configuration (VoiceS2SConfig or dict). If None, uses default configuration (no deployment_id).</p> <code>None</code>"},{"location":"api/cx_agent_studio/#tac_google.connectors.cx_agent_studio.voice_s2s.channel.VoiceS2SChannel.handle_incoming_call","title":"handle_incoming_call  <code>async</code>","text":"<pre><code>handle_incoming_call(websocket_url: str | None = None) -&gt; str\n</code></pre> <p>Generate TwiML that connects the call to Twilio Media Streams.</p> <p>Unlike ConversationRelay, this carries raw audio \u2014 CES does its own speech recognition/synthesis over <code>BidiRunSession</code>.</p> <p>TODO: only <code>websocket_url</code> is configurable right now. We'll likely want to expose more <code>&lt;Stream&gt;</code>/<code>&lt;Connect&gt;</code> TwiML options later (e.g. <code>name</code>, <code>status_callback</code>, custom <code>&lt;Parameter&gt;</code>s) \u2014 see <code>VoiceChannel</code>'s layered <code>TwiMLOptions</code> for the pattern to follow.</p> <p>Parameters:</p> Name Type Description Default <code>websocket_url</code> <code>str | None</code> <p>Per-call override for the WebSocket URL, supplied by the host (the code owning the route \u2014 e.g. a custom server) for transport facts this channel can't derive, such as a per-call affinity token. Takes precedence over the <code>TACConfig.voice_public_domain</code>-derived default.</p> <code>None</code>"},{"location":"api/cx_agent_studio/#tac_google.connectors.cx_agent_studio.voice_s2s.channel.VoiceS2SChannel.handle_websocket","title":"handle_websocket  <code>async</code>","text":"<pre><code>handle_websocket(websocket: WebSocketProtocol) -&gt; None\n</code></pre> <p>Bridge one call's Twilio Media Streams connection to a CES <code>BidiRunSession</code> websocket for the lifetime of the call.</p> <p>Opens the CES connection once Twilio's <code>start</code> event arrives (the CES session id is derived from the Twilio call sid), then runs Twilio\u2192CES and CES\u2192Twilio concurrently until either side ends the call \u2014 the first to finish cancels the other.</p>"},{"location":"api/cx_agent_studio/#tac_google.connectors.cx_agent_studio.voice_s2s.channel.VoiceS2SChannel.process_webhook","title":"process_webhook  <code>async</code>","text":"<pre><code>process_webhook(webhook_data: dict[str, Any], idempotency_token: str | None = None) -&gt; None\n</code></pre> <p>Not used \u2014 S2S has no Conversation Orchestrator text-turn webhook.</p>"},{"location":"api/cx_agent_studio/#tac_google.connectors.cx_agent_studio.voice_s2s.channel.VoiceS2SChannel.send_response","title":"send_response  <code>async</code>","text":"<pre><code>send_response(conversation_id: str, response: Any, role: str | None = None) -&gt; None\n</code></pre> <p>Not used \u2014 audio is forwarded directly inside <code>handle_websocket</code>, there's no discrete turn to push a response for.</p>"},{"location":"api/cx_agent_studio/#tac_google.connectors.cx_agent_studio.voice_s2s.config","title":"tac_google.connectors.cx_agent_studio.voice_s2s.config","text":"<p>Voice S2S channel configuration.</p>"},{"location":"api/cx_agent_studio/#tac_google.connectors.cx_agent_studio.voice_s2s.config.VoiceS2SConfig","title":"VoiceS2SConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Configuration for <code>VoiceS2SChannel</code>.</p> <p>Attributes:</p> Name Type Description <code>deployment_id</code> <code>str | None</code> <p>Optional CES deployment resource name, for agents with a dedicated Twilio channel deployment in the CES console.</p> <code>welcome_greeting</code> <code>str | None</code> <p>Text sent to CES as the session-kickoff turn. Unlike ConversationRelay's <code>welcome_greeting</code>, this isn't spoken verbatim \u2014 the agent decides how to respond to it, like any other turn. Set to <code>None</code> to let the agent's own configured greeting play unmodified.</p>"},{"location":"deploy/","title":"Deployment Guide","text":"<p>Deploy AI agents to GCP and connect them to Twilio with Twilio Agent Connect (TAC), Twilio's framework for connecting AI agents to voice and messaging channels.</p>"},{"location":"deploy/#deployment-methods","title":"Deployment methods","text":"<ul> <li>Agent Platform Runtime \u2014 deploy your agent to GCP's   managed Agent Platform Runtime (Reasoning Engine) and connect it to Twilio.</li> <li>CX Agent Studio \u2014 connect Twilio to an agent built   in CX Agent Studio (Customer Engagement Suite).</li> <li>Conversational Agents \u2014 connect Twilio to an   agent built in Conversational Agents (Dialogflow CX).</li> </ul>"},{"location":"deploy/agent_platform/","title":"Connect Twilio to an agent on Agent Platform Runtime","text":"<p>Deploy Twilio Agent Connect (TAC) \u2014 Twilio's framework for connecting AI agents to voice and messaging channels \u2014 with an agent on GCP Agent Platform Runtime and the TAC server on Cloud Run. This directory has one fully independent deployment per agent type \u2014 pick one:</p> <ul> <li><code>adk/</code> \u2014 an ADK agent, connected via <code>ADKAgentEngineConnector</code>.</li> <li><code>studio/</code> \u2014 a source-code app built and deployed from the   Agent Studio console, connected via <code>StudioAgentEngineConnector</code>.</li> </ul> <p>Each folder has its own agent, TAC server, Cloud Run deploy, Secret Manager setup, <code>.env</code>, and <code>Makefile</code> \u2014 nothing is shared between them, so you can follow either one on its own. See the <code>README.md</code> inside each for setup steps.</p>"},{"location":"deploy/agent_platform/adk/","title":"Connect Twilio to an ADK agent","text":"<p>Deploy Twilio Agent Connect (TAC) \u2014 Twilio's framework for connecting AI agents to voice and messaging channels \u2014 with an ADK agent on GCP Agent Platform Runtime and the TAC server on Cloud Run.</p> <p>The flow has two independently deployed pieces: - Agent (<code>agent/</code>) \u2014 an ADK agent, hosted on Agent Platform Runtime   (a.k.a. Reasoning Engine). - TAC server (<code>server/</code>) \u2014 a FastAPI app that terminates Twilio webhooks +   the ConversationRelay WebSocket and forwards turns to the agent, on Cloud Run.</p> <p>The agent has no knowledge of Twilio; the server has no LLM logic. <code>ADKAgentEngineConnector</code> is the seam between them.</p>"},{"location":"deploy/agent_platform/adk/#architecture","title":"Architecture","text":"<pre><code>graph TB\n    Customer([\ud83d\udc64 Customer&lt;br/&gt;Phone Call / SMS])\n\n    subgraph Twilio[\"\u2601\ufe0f Twilio Cloud\"]\n        Phone[\ud83d\udcf1 Phone Number&lt;br/&gt;+1-XXX-XXX-XXXX]\n        Orchestrator[\ud83d\udcac Conversations&lt;br/&gt;Conversation Orchestrator]\n        Memory[\ud83e\udde0 Memory Service&lt;br/&gt;Profile &amp; Context]\n    end\n\n    subgraph GCP[\"\u2601\ufe0f Google Cloud\"]\n        subgraph CloudRun[\"\ud83c\udfc3 Cloud Run\"]\n            Server[\u2699\ufe0f TAC Server&lt;br/&gt;WebSocket/HTTP]\n        end\n\n        subgraph APR[\"\ud83e\udd16 Agent Platform Runtime\"]\n            Agent[\ud83e\udde0 ADK Agent&lt;br/&gt;async_stream_query]\n        end\n\n        Secret[\ud83d\udd10 Secret Manager&lt;br/&gt;Twilio credentials]\n    end\n\n    %% Voice Channel Flow (A-D)\n    Customer --&gt;|A. Phone Call| Phone\n    Phone --&gt;|B. POST /twiml| Server\n    Server --&gt;|C. TwiML with&lt;br/&gt;wss:// WebSocket URL| Phone\n    Phone &lt;--&gt;|D. Twilio ConversationRelay| Server\n\n    %% Messaging Channel Flow (1-4)\n    Customer --&gt;|1. SMS| Phone\n    Phone --&gt;|2. POST /webhook| Server\n    Server --&gt;|3. Forward to Agent| Agent\n    Agent --&gt;|4. SMS Response| Phone\n\n    %% Cloud Run integrations\n    Server --&gt; Agent\n    Server -.-&gt;|reads credentials| Secret\n    Agent --&gt; Orchestrator\n    Agent --&gt; Memory\n\n    Phone --&gt;|Response| Customer\n\n    style Customer fill:#e1f5ff\n    style Twilio fill:#f0f0f0\n    style GCP fill:#e8f0fe\n    style CloudRun fill:#e6f4ea\n    style APR fill:#f3e5f5\n    style Agent fill:#e1bee7\n    style Secret fill:#fff4e6</code></pre>"},{"location":"deploy/agent_platform/adk/#deployment-components","title":"Deployment Components","text":"<ul> <li>Agent - the ADK agent, hosted on Agent Platform Runtime; deployed from <code>agent/</code></li> <li>Cloud Run Service - the TAC server (FastAPI), HTTP webhooks and WebSocket endpoints; deployed from <code>server/</code></li> <li>Artifact Registry - holds the server's container image (created automatically)</li> <li>Secret Manager - holds the Twilio credentials, read by the Cloud Run service</li> </ul>"},{"location":"deploy/agent_platform/adk/#prerequisites","title":"Prerequisites","text":""},{"location":"deploy/agent_platform/adk/#required-tools","title":"Required Tools","text":"<ul> <li>Google Cloud CLI - Command-line tool for GCP (<code>brew install --cask google-cloud-sdk</code>)</li> <li>Python 3.11 - to run the agent deploy scripts (Agent Platform Runtime requires 3.11)</li> </ul>"},{"location":"deploy/agent_platform/adk/#gcp-account-requirements","title":"GCP Account Requirements","text":"<ul> <li>Active GCP Project with billing enabled and access to:</li> <li>Vertex AI (Agent Platform Runtime) \u2014 enable it before deploying: <code>gcloud services enable aiplatform.googleapis.com</code></li> <li>Cloud Run, Cloud Build, and Artifact Registry (enabled automatically by <code>server/deploy.sh</code>)</li> <li>Gemini 3.5 Flash enabled in Model Garden</li> <li>Region: any region with Vertex AI and Cloud Run availability \u2014 set it via <code>GOOGLE_CLOUD_LOCATION</code> in <code>.env</code></li> </ul>"},{"location":"deploy/agent_platform/adk/#quick-start","title":"Quick Start","text":""},{"location":"deploy/agent_platform/adk/#1-configure-environment","title":"1. Configure Environment","text":"<p>The agent scripts and the server share a single <code>.env</code> in this folder:</p> <pre><code>cd deploy/agent_platform/adk\ncp .env.example .env\n</code></pre> <p>Edit <code>.env</code> with your values (leave <code>GCP_REASONING_ENGINE_ID</code> blank \u2014 the agent deploy fills it in):</p> <pre><code># GCP Configuration\nGOOGLE_CLOUD_PROJECT=your-project-id\nGOOGLE_CLOUD_LOCATION=your-region\nGCP_REASONING_ENGINE_ID=                            # filled in by the agent deploy\n\n# Twilio Credentials\nTWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\nTWILIO_AUTH_TOKEN=your_auth_token\nTWILIO_API_KEY=SKxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\nTWILIO_API_SECRET=your_api_secret\n\n# Twilio Configuration\nTWILIO_PHONE_NUMBER=+1234567890\nTWILIO_CONVERSATION_CONFIGURATION_ID=conv_configuration_xxxx\n</code></pre> <p>Where to find Twilio values: - Account SID &amp; Auth Token: Twilio Console \u2192 Account \u2192 API keys &amp; tokens - API Key &amp; Secret: Create new API Key - Phone Number: Twilio Console \u2192 Phone Numbers - Conversation Configuration ID: Twilio Console \u2192 Conversation Orchestrator</p> <p>The four Twilio credentials (<code>TWILIO_ACCOUNT_SID</code>, <code>TWILIO_AUTH_TOKEN</code>, <code>TWILIO_API_KEY</code>, <code>TWILIO_API_SECRET</code>) are stored in Secret Manager by the deploy; the rest are passed as env vars.</p>"},{"location":"deploy/agent_platform/adk/#2-authenticate-one-time","title":"2. Authenticate (one-time)","text":"<p>Run once per machine \u2014 the credentials persist for future deploys. The project comes from <code>.env</code>, so you don't need <code>gcloud config set project</code>.</p> <pre><code>gcloud auth login                        # gcloud CLI (server deploy)\ngcloud auth application-default login    # ADC (agent deploy scripts)\n</code></pre>"},{"location":"deploy/agent_platform/adk/#3-create-the-deploy-service-account-one-time","title":"3. Create the deploy service account (one-time)","text":"<p>Creates the dedicated <code>tac-deployer</code> SA (derived from your project \u2014 nothing to configure). The deploy grants it the roles it needs.</p> <pre><code>make create-sa\n</code></pre>"},{"location":"deploy/agent_platform/adk/#4-deploy","title":"4. Deploy","text":"<p>One command deploys the agent, stores the secrets, and deploys the server:</p> <pre><code>make deploy-all\n</code></pre> <p><code>deploy-all</code> runs <code>deploy-agent</code> (which writes the new agent ID into <code>.env</code>), then <code>deploy-secret</code>, then <code>deploy-server</code>. Or run the steps individually: <code>make deploy-agent</code> / <code>make deploy-secret</code> / <code>make deploy-server</code>.</p> <p>Deployment output:</p> <pre><code>\u2705 Deployed: https://tac-server-adk-xxxxx.&lt;region&gt;.run.app\n\nConfigure Twilio:\n  Conversation Orchestrator status callback : https://tac-server-adk-xxxxx.&lt;region&gt;.run.app/webhook   (POST)\n  Voice \"A call comes in\" webhook           : https://tac-server-adk-xxxxx.&lt;region&gt;.run.app/twiml     (POST)\n</code></pre> <p>Copy these webhook URLs for Twilio configuration. Re-running <code>make deploy-server</code> keeps the same URL.</p>"},{"location":"deploy/agent_platform/adk/#twilio-configuration","title":"Twilio Configuration","text":""},{"location":"deploy/agent_platform/adk/#configure-voice-webhook-phone-number","title":"Configure Voice Webhook (Phone Number)","text":"<ol> <li>Go to Twilio Console \u2192 Phone Numbers \u2192 Active Numbers</li> <li>Select your phone number</li> <li>Under \"Voice Configuration\":</li> <li>A CALL COMES IN: Webhook</li> <li>URL: Use the <code>/twiml</code> URL from deploy output</li> <li>HTTP Method: POST</li> <li>Save</li> </ol>"},{"location":"deploy/agent_platform/adk/#configure-conversation-webhook-smsmessaging","title":"Configure Conversation Webhook (SMS/Messaging)","text":"<ol> <li>Go to Twilio Console \u2192 Conversation Orchestrator</li> <li>Select your Conversation Configuration</li> <li>Under \"Webhook Configuration\":</li> <li>Webhook URL: Use the <code>/webhook</code> URL from deploy output</li> <li>HTTP Method: POST</li> <li>Save</li> </ol> <p>Note: SMS flows through the Conversation Orchestrator status callback, not the phone number's \"A message comes in\" webhook. Setting the phone-number messaging webhook does nothing here \u2014 you must update the Conversation Configuration above.</p>"},{"location":"deploy/agent_platform/adk/#view-logs","title":"View Logs","text":""},{"location":"deploy/agent_platform/adk/#cloud-run-logs","title":"Cloud Run Logs","text":"<p>Application logs (startup, agent calls, errors):</p> <pre><code>gcloud run services logs read tac-server-adk --project your-project-id --region your-region --limit 50\n</code></pre> <p>Or go to the Cloud Logging Console, and filter by Cloud Run Revision \u2192 <code>tac-server-adk</code>.</p>"},{"location":"deploy/agent_platform/adk/#request-logs","title":"Request Logs","text":"<p>To confirm whether Twilio's webhook is reaching the service (and with what status code):</p> <pre><code>gcloud logging read \\\n  'resource.type=\"cloud_run_revision\" AND resource.labels.service_name=\"tac-server-adk\" AND httpRequest.requestUrl!=\"\"' \\\n  --project your-project-id --limit 25 --freshness=1h \\\n  --format='table(timestamp, httpRequest.requestMethod, httpRequest.status, httpRequest.requestUrl)'\n</code></pre>"},{"location":"deploy/agent_platform/adk/#update-code","title":"Update Code","text":"<ul> <li>After editing the agent: <code>make deploy-agent</code>.</li> <li>After editing the server or the package: <code>make deploy-server</code>.</li> <li>After rotating Twilio credentials in <code>.env</code>: <code>make deploy-secret</code>.</li> </ul>"},{"location":"deploy/agent_platform/studio/","title":"Connect Twilio to an Agent Studio app","text":"<p>Deploy Twilio Agent Connect (TAC) \u2014 Twilio's framework for connecting AI agents to voice and messaging channels \u2014 with an Agent Studio app on GCP Agent Platform Runtime and the TAC server on Cloud Run.</p> <p>The flow has two independently deployed pieces: - Agent (<code>agent/</code>) \u2014 a source-code app built and deployed from the Agent   Studio console (no <code>deploy.py</code> \u2014 see <code>agent/README.md</code>),   hosted on Agent Platform Runtime (a.k.a. Reasoning Engine). - TAC server (<code>server/</code>) \u2014 a FastAPI app that terminates Twilio webhooks +   the ConversationRelay WebSocket and forwards turns to the agent, on Cloud Run.</p> <p>The agent has no knowledge of Twilio; the server has no LLM logic. <code>StudioAgentEngineConnector</code> is the seam between them \u2014 it talks to the agent over the fixed <code>reasoningEngines:streamQuery</code> REST endpoint, since Studio apps don't register SDK <code>query</code>/<code>stream_query</code> methods.</p>"},{"location":"deploy/agent_platform/studio/#architecture","title":"Architecture","text":"<pre><code>graph TB\n    Customer([\ud83d\udc64 Customer&lt;br/&gt;Phone Call / SMS])\n\n    subgraph Twilio[\"\u2601\ufe0f Twilio Cloud\"]\n        Phone[\ud83d\udcf1 Phone Number&lt;br/&gt;+1-XXX-XXX-XXXX]\n        Orchestrator[\ud83d\udcac Conversations&lt;br/&gt;Conversation Orchestrator]\n        Memory[\ud83e\udde0 Memory Service&lt;br/&gt;Profile &amp; Context]\n    end\n\n    subgraph GCP[\"\u2601\ufe0f Google Cloud\"]\n        subgraph CloudRun[\"\ud83c\udfc3 Cloud Run\"]\n            Server[\u2699\ufe0f TAC Server&lt;br/&gt;WebSocket/HTTP]\n        end\n\n        subgraph APR[\"\ud83e\udd16 Agent Platform Runtime\"]\n            Agent[\ud83e\udde0 Agent Studio App&lt;br/&gt;streamQuery REST]\n        end\n\n        Secret[\ud83d\udd10 Secret Manager&lt;br/&gt;Twilio credentials]\n    end\n\n    %% Voice Channel Flow (A-D)\n    Customer --&gt;|A. Phone Call| Phone\n    Phone --&gt;|B. POST /twiml| Server\n    Server --&gt;|C. TwiML with&lt;br/&gt;wss:// WebSocket URL| Phone\n    Phone &lt;--&gt;|D. Twilio ConversationRelay| Server\n\n    %% Messaging Channel Flow (1-4)\n    Customer --&gt;|1. SMS| Phone\n    Phone --&gt;|2. POST /webhook| Server\n    Server --&gt;|3. Forward to Agent| Agent\n    Agent --&gt;|4. SMS Response| Phone\n\n    %% Cloud Run integrations\n    Server --&gt; Agent\n    Server -.-&gt;|reads credentials| Secret\n    Agent --&gt; Orchestrator\n    Agent --&gt; Memory\n\n    Phone --&gt;|Response| Customer\n\n    style Customer fill:#e1f5ff\n    style Twilio fill:#f0f0f0\n    style GCP fill:#e8f0fe\n    style CloudRun fill:#e6f4ea\n    style APR fill:#f3e5f5\n    style Agent fill:#e1bee7\n    style Secret fill:#fff4e6</code></pre>"},{"location":"deploy/agent_platform/studio/#deployment-components","title":"Deployment Components","text":"<ul> <li>Agent - the Agent Studio app, hosted on Agent Platform Runtime; built and deployed from the console \u2014 see <code>agent/README.md</code></li> <li>Cloud Run Service - the TAC server (FastAPI), HTTP webhooks and WebSocket endpoints; deployed from <code>server/</code></li> <li>Artifact Registry - holds the server's container image (created automatically)</li> <li>Secret Manager - holds the Twilio credentials, read by the Cloud Run service</li> </ul>"},{"location":"deploy/agent_platform/studio/#prerequisites","title":"Prerequisites","text":""},{"location":"deploy/agent_platform/studio/#required-tools","title":"Required Tools","text":"<ul> <li>Google Cloud CLI - Command-line tool for GCP (<code>brew install --cask google-cloud-sdk</code>)</li> <li>Python 3.11 - to run the invoke test client (Agent Platform Runtime requires 3.11)</li> </ul>"},{"location":"deploy/agent_platform/studio/#gcp-account-requirements","title":"GCP Account Requirements","text":"<ul> <li>Active GCP Project with billing enabled and access to:</li> <li>Vertex AI (Agent Platform Runtime) \u2014 enable it before deploying: <code>gcloud services enable aiplatform.googleapis.com cloudresourcemanager.googleapis.com</code></li> <li>Cloud Run, Cloud Build, and Artifact Registry (enabled automatically by <code>server/deploy.sh</code>)</li> <li>Region: Agent Studio currently stores and deploys agents in us-west1 only \u2014 set <code>GOOGLE_CLOUD_LOCATION=us-west1</code> in <code>.env</code></li> </ul>"},{"location":"deploy/agent_platform/studio/#quick-start","title":"Quick Start","text":""},{"location":"deploy/agent_platform/studio/#1-build-and-deploy-the-agent","title":"1. Build and Deploy the Agent","text":"<p>Build and deploy the agent in the Agent Studio console \u2014 see <code>agent/README.md</code>. Save the deployed agent's resource ID.</p>"},{"location":"deploy/agent_platform/studio/#2-configure-environment","title":"2. Configure Environment","text":"<p>The invoke test client and the server share a single <code>.env</code> in this folder:</p> <pre><code>cd deploy/agent_platform/studio\ncp .env.example .env\n</code></pre> <p>Edit <code>.env</code> with your values:</p> <pre><code># GCP Configuration\nGOOGLE_CLOUD_PROJECT=your-project-id\nGOOGLE_CLOUD_LOCATION=us-west1\nGCP_REASONING_ENGINE_ID=your-agent-id\n\n# Twilio Credentials\nTWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\nTWILIO_AUTH_TOKEN=your_auth_token\nTWILIO_API_KEY=SKxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\nTWILIO_API_SECRET=your_api_secret\n\n# Twilio Configuration\nTWILIO_PHONE_NUMBER=+1234567890\nTWILIO_CONVERSATION_CONFIGURATION_ID=conv_configuration_xxxx\n</code></pre> <p>Where to find Twilio values: - Account SID &amp; Auth Token: Twilio Console \u2192 Account \u2192 API keys &amp; tokens - API Key &amp; Secret: Create new API Key - Phone Number: Twilio Console \u2192 Phone Numbers - Conversation Configuration ID: Twilio Console \u2192 Conversation Orchestrator</p> <p>The four Twilio credentials (<code>TWILIO_ACCOUNT_SID</code>, <code>TWILIO_AUTH_TOKEN</code>, <code>TWILIO_API_KEY</code>, <code>TWILIO_API_SECRET</code>) are stored in Secret Manager by the deploy; the rest are passed as env vars.</p>"},{"location":"deploy/agent_platform/studio/#3-authenticate-one-time","title":"3. Authenticate (one-time)","text":"<p>Run once per machine \u2014 the credentials persist for future deploys. The project comes from <code>.env</code>, so you don't need <code>gcloud config set project</code>.</p> <pre><code>gcloud auth login                        # gcloud CLI (server deploy)\ngcloud auth application-default login    # ADC (invoke test client)\n</code></pre>"},{"location":"deploy/agent_platform/studio/#4-create-the-deploy-service-account-one-time","title":"4. Create the deploy service account (one-time)","text":"<p>Creates the dedicated <code>tac-deployer</code> SA (derived from your project \u2014 nothing to configure). The deploy grants it the roles it needs.</p> <pre><code>make create-sa\n</code></pre>"},{"location":"deploy/agent_platform/studio/#5-deploy","title":"5. Deploy","text":"<pre><code>make deploy-all\n</code></pre> <p><code>deploy-all</code> runs <code>deploy-secret</code>, then <code>deploy-server</code>. Or run the steps individually: <code>make deploy-secret</code> / <code>make deploy-server</code>.</p> <p>Deployment output:</p> <pre><code>\u2705 Deployed: https://tac-server-studio-xxxxx.&lt;region&gt;.run.app\n\nConfigure Twilio:\n  Conversation Orchestrator status callback : https://tac-server-studio-xxxxx.&lt;region&gt;.run.app/webhook   (POST)\n  Voice \"A call comes in\" webhook           : https://tac-server-studio-xxxxx.&lt;region&gt;.run.app/twiml     (POST)\n</code></pre> <p>Copy these webhook URLs for Twilio configuration. Re-running <code>make deploy-server</code> keeps the same URL.</p>"},{"location":"deploy/agent_platform/studio/#twilio-configuration","title":"Twilio Configuration","text":""},{"location":"deploy/agent_platform/studio/#configure-voice-webhook-phone-number","title":"Configure Voice Webhook (Phone Number)","text":"<ol> <li>Go to Twilio Console \u2192 Phone Numbers \u2192 Active Numbers</li> <li>Select your phone number</li> <li>Under \"Voice Configuration\":</li> <li>A CALL COMES IN: Webhook</li> <li>URL: Use the <code>/twiml</code> URL from deploy output</li> <li>HTTP Method: POST</li> <li>Save</li> </ol>"},{"location":"deploy/agent_platform/studio/#configure-conversation-webhook-smsmessaging","title":"Configure Conversation Webhook (SMS/Messaging)","text":"<ol> <li>Go to Twilio Console \u2192 Conversation Orchestrator</li> <li>Select your Conversation Configuration</li> <li>Under \"Webhook Configuration\":</li> <li>Webhook URL: Use the <code>/webhook</code> URL from deploy output</li> <li>HTTP Method: POST</li> <li>Save</li> </ol> <p>Note: SMS flows through the Conversation Orchestrator status callback, not the phone number's \"A message comes in\" webhook. Setting the phone-number messaging webhook does nothing here \u2014 you must update the Conversation Configuration above.</p>"},{"location":"deploy/agent_platform/studio/#view-logs","title":"View Logs","text":""},{"location":"deploy/agent_platform/studio/#cloud-run-logs","title":"Cloud Run Logs","text":"<p>Application logs (startup, agent calls, errors):</p> <pre><code>gcloud run services logs read tac-server-studio --project your-project-id --region your-region --limit 50\n</code></pre> <p>Or go to the Cloud Logging Console, and filter by Cloud Run Revision \u2192 <code>tac-server-studio</code>.</p>"},{"location":"deploy/agent_platform/studio/#request-logs","title":"Request Logs","text":"<p>To confirm whether Twilio's webhook is reaching the service (and with what status code):</p> <pre><code>gcloud logging read \\\n  'resource.type=\"cloud_run_revision\" AND resource.labels.service_name=\"tac-server-studio\" AND httpRequest.requestUrl!=\"\"' \\\n  --project your-project-id --limit 25 --freshness=1h \\\n  --format='table(timestamp, httpRequest.requestMethod, httpRequest.status, httpRequest.requestUrl)'\n</code></pre>"},{"location":"deploy/agent_platform/studio/#update-code","title":"Update Code","text":"<ul> <li>After editing the agent: redeploy from the Agent Studio console.</li> <li>After editing the server or the package: <code>make deploy-server</code>.</li> <li>After rotating Twilio credentials in <code>.env</code>: <code>make deploy-secret</code>.</li> </ul>"},{"location":"deploy/agent_platform/studio/agent/","title":"Agent Studio","text":"<p>Agent Studio is a low-code visual builder in the Google Cloud console. You design the agent in the UI and deploy it from there \u2014 there is no <code>deploy.py</code> here. Under the hood it produces a source-code app on Agent Platform Runtime (Agent Engine), the same runtime the <code>adk/</code> example uses.</p>"},{"location":"deploy/agent_platform/studio/agent/#create-an-agent","title":"Create an agent","text":"<p>Prerequisites (one-time, per project):</p> <pre><code>gcloud services enable aiplatform.googleapis.com cloudresourcemanager.googleapis.com\n</code></pre> <p>Then in the console:</p> <ol> <li>Go to Agent Platform \u2192 Studio \u2192 Agents and click Create agent.</li> <li>Set the instructions, model, and any tools (Google Search, URL    context, MCP, sub-agents).</li> <li>Use the Preview tab to chat-test the agent.</li> <li>Click Save (Studio currently stores and deploys agents in us-west1    only).</li> <li>Click Deploy, pick the region, and confirm. Deployment creates an Agent    Runtime instance and takes a few minutes.</li> <li>Open the deployed agent and copy its resource ID (the number in    <code>.../reasoningEngines/&lt;ID&gt;</code>).</li> </ol>"},{"location":"deploy/agent_platform/studio/agent/#configure","title":"Configure","text":"<p>In <code>../.env</code>, set the deployed agent's ID and point the location at us-west1 (where Studio deploys):</p> <pre><code>GCP_REASONING_ENGINE_ID=&lt;your-agent-id&gt;\nGOOGLE_CLOUD_LOCATION=us-west1\n</code></pre>"},{"location":"deploy/agent_platform/studio/agent/#invoke","title":"Invoke","text":"<pre><code>python invoke.py\n</code></pre> <p><code>invoke.py</code> calls the <code>reasoningEngines:streamQuery</code> REST endpoint directly. Studio deploys a source-code app that does not register the SDK <code>query</code>/<code>stream_query</code> methods, so the SDK's dynamically-bound client methods don't apply here \u2014 this is also why <code>StudioAgentEngineConnector</code> (used by <code>../server/main.py</code>) talks to it over the same REST endpoint.</p>"},{"location":"deploy/conversational_agents/","title":"Connect Twilio to a Conversational Agents (Dialogflow CX) agent","text":"<p>Connect Twilio Voice and SMS to an agent built in Conversational Agents (Dialogflow CX). The agent is built in the console; a Twilio Agent Connect (TAC) server \u2014 TAC being Twilio's framework for connecting AI agents to voice and messaging channels \u2014 runs on Cloud Run and bridges Twilio to it over the Dialogflow CX <code>detectIntent</code> API.</p> <p>The agent has no knowledge of Twilio; the server has no LLM logic. The <code>ConversationalAgentsConnector</code> is the seam between them. Twilio ConversationRelay does the speech, so only text is exchanged and Dialogflow keeps the conversation history server-side (per session id).</p>"},{"location":"deploy/conversational_agents/#architecture","title":"Architecture","text":"<pre><code>graph TB\n    Customer([\ud83d\udc64 Customer&lt;br/&gt;Phone Call / SMS])\n\n    subgraph Twilio[\"\u2601\ufe0f Twilio Cloud\"]\n        Phone[\ud83d\udcf1 Phone Number]\n        Orchestrator[\ud83d\udcac Conversation Orchestrator]\n        Memory[\ud83e\udde0 Memory Service]\n    end\n\n    subgraph GCP[\"\u2601\ufe0f Google Cloud\"]\n        subgraph CloudRun[\"\ud83c\udfc3 Cloud Run\"]\n            Server[\u2699\ufe0f TAC Server&lt;br/&gt;WebSocket/HTTP]\n        end\n        Agent[\ud83e\udde0 Conversational Agents&lt;br/&gt;Dialogflow CX detectIntent]\n        Secret[\ud83d\udd10 Secret Manager&lt;br/&gt;Twilio credentials]\n    end\n\n    %% Voice Channel Flow (A-D)\n    Customer --&gt;|A. Phone Call| Phone\n    Phone --&gt;|B. POST /twiml| Server\n    Server --&gt;|C. TwiML wss:// URL| Phone\n    Phone &lt;--&gt;|D. ConversationRelay text| Server\n\n    %% Messaging Channel Flow (1-7)\n    Customer --&gt;|1. SMS| Phone\n    Phone --&gt;|2| Orchestrator\n    Orchestrator --&gt;|3. POST /webhook&lt;br/&gt;status callback| Server\n    Server --&gt;|4. detectIntent| Agent\n    Agent --&gt;|5. Response| Server\n    Server --&gt;|6. SMS Response via&lt;br/&gt;Conversations API| Orchestrator\n    Orchestrator --&gt;|7| Phone\n\n    %% Cloud Run integrations\n    Server -.-&gt;|reads credentials| Secret\n    Server --&gt; Memory\n    Phone --&gt;|Response| Customer\n\n    style Customer fill:#e1f5ff\n    style Twilio fill:#f0f0f0\n    style GCP fill:#e8f0fe\n    style CloudRun fill:#e6f4ea\n    style Agent fill:#e1bee7\n    style Secret fill:#fff4e6</code></pre>"},{"location":"deploy/conversational_agents/#deployment-components","title":"Deployment Components","text":"<ul> <li>Agent - built in the Conversational Agents console; see <code>agent/</code></li> <li>Cloud Run Service - the TAC server (FastAPI), HTTP webhooks and WebSocket endpoints; deployed from <code>server/</code></li> <li>Artifact Registry - holds the server's container image (created automatically)</li> <li>Secret Manager - holds the Twilio credentials, read by the Cloud Run service</li> </ul>"},{"location":"deploy/conversational_agents/#prerequisites","title":"Prerequisites","text":"<ul> <li>Google Cloud CLI installed and authenticated</li> <li>An agent built in Conversational Agents (see <code>agent/</code>)</li> <li>A GCP project with Cloud Run, Cloud Build, and Artifact Registry available (enabled automatically by <code>server/deploy.sh</code>)</li> <li>The Cloud Run runtime service account needs <code>roles/dialogflow.client</code> to call the agent (granted by <code>server/deploy.sh</code>)</li> </ul>"},{"location":"deploy/conversational_agents/#quick-start","title":"Quick Start","text":""},{"location":"deploy/conversational_agents/#1-build-the-agent","title":"1. Build the agent","text":"<p>Follow <code>agent/README.md</code> to build the agent in the console and copy its resource name.</p>"},{"location":"deploy/conversational_agents/#2-configure-environment","title":"2. Configure environment","text":"<pre><code>cd deploy/conversational_agents\ncp .env.example .env\n# fill in GOOGLE_CLOUD_PROJECT, GOOGLE_CLOUD_LOCATION, CONVERSATIONAL_AGENT_ID, and Twilio creds\n</code></pre>"},{"location":"deploy/conversational_agents/#3-authenticate-one-time","title":"3. Authenticate (one-time)","text":"<pre><code>gcloud auth login\ngcloud auth application-default login\n</code></pre>"},{"location":"deploy/conversational_agents/#4-create-the-deploy-service-account-one-time","title":"4. Create the deploy service account (one-time)","text":"<pre><code>make create-sa\n</code></pre>"},{"location":"deploy/conversational_agents/#5-deploy","title":"5. Deploy","text":"<pre><code>make deploy-all      # store Twilio secrets, then build + deploy the server\n</code></pre> <p>Or run the steps individually: <code>make deploy-secret</code> / <code>make deploy-server</code>.</p> <p><code>deploy-server</code> prints the webhook URLs to set in Twilio:</p> <ul> <li>Conversation Orchestrator status callback \u2192 <code>https://&lt;service-url&gt;/webhook</code></li> <li>Voice \"A call comes in\" \u2192 <code>https://&lt;service-url&gt;/twiml</code></li> </ul>"},{"location":"deploy/conversational_agents/#twilio-configuration","title":"Twilio Configuration","text":"<ul> <li>Voice (Phone Number \u2192 \"A call comes in\"): the <code>/twiml</code> URL, POST.</li> <li>SMS (Conversation Orchestrator \u2192 your configuration \u2192 status callback): the <code>/webhook</code> URL, POST. SMS flows through the Conversation Orchestrator status callback, not the phone number's \"A message comes in\" webhook.</li> </ul>"},{"location":"deploy/conversational_agents/#view-logs","title":"View Logs","text":"<pre><code>gcloud run services logs read tac-df-server --project your-project-id --region your-region --limit 50\n</code></pre>"},{"location":"deploy/conversational_agents/agent/","title":"Conversational Agents (Dialogflow CX) agent","text":"<p>Conversational Agents is Google's Dialogflow CX platform. You build the agent in the console; it is invocable immediately (no deploy needed) over the Dialogflow CX <code>detectIntent</code> API. The Twilio Agent Connect (TAC) server connects Twilio to it over that same API.</p>"},{"location":"deploy/conversational_agents/agent/#build-the-agent","title":"Build the agent","text":"<ol> <li>Open the Conversational Agents console    and select your project.</li> <li>Click Create agent \u2192 Build your own.</li> <li>Set the region, time zone, and language, then choose how the conversation    starts:</li> <li>Playbook \u2014 generative (Gemini) agent driven by natural-language instructions.</li> <li>Flow \u2014 deterministic state machine (pages, intents, transitions).</li> </ol> <p>Both are invoked the same way, so either works. For a quick start, pick    Playbook and give it a short instruction (e.g. \"You are a helpful    assistant. Be concise.\"). 4. Use the Preview / simulator on the right to chat-test the agent.</p> <p>No deploy step is required \u2014 the draft agent responds to <code>detectIntent</code> right away. (Deploying to an environment is only for pinning a version for production.)</p>"},{"location":"deploy/conversational_agents/agent/#get-the-agent-id","title":"Get the agent ID","text":"<p>The connector needs the agent's resource name:</p> <pre><code>projects/&lt;project&gt;/locations/&lt;location&gt;/agents/&lt;agent-id&gt;\n</code></pre> <p>It is in the console URL, e.g. <code>https://conversational-agents.cloud.google.com/projects/&lt;project&gt;/locations/&lt;location&gt;/agents/&lt;agent-id&gt;/...</code>. Put it in <code>.env</code>:</p> <pre><code>CONVERSATIONAL_AGENT_ID=projects/your-project/locations/us-central1/agents/your-agent-id\n</code></pre>"},{"location":"deploy/conversational_agents/agent/#invoke","title":"Invoke","text":"<pre><code>python invoke.py\n</code></pre> <p><code>invoke.py</code> calls <code>POST https://&lt;location&gt;-dialogflow.googleapis.com/v3/&lt;agent&gt;/sessions/&lt;session&gt;:detectIntent</code> with <code>{\"queryInput\": {\"text\": {\"text\": \"...\"}, \"languageCode\": \"en\"}}</code> and reads the reply from <code>queryResult.responseMessages[].text.text</code>.</p> <p>Authentication uses Application Default Credentials \u2014 grant the calling principal (or Cloud Run service account) the Dialogflow API Client role. User ADC also needs a quota project, which <code>invoke.py</code> sets via the <code>X-Goog-User-Project</code> header.</p>"},{"location":"deploy/cx_agent_studio/","title":"Connect Twilio to CX Agent Studio","text":"<p>The Twilio Agent Connect (TAC) SDK connects AI agents to Twilio's voice and messaging channels. This deploys it on Cloud Run, wired to an agent built in CX Agent Studio (Customer Engagement Suite).</p> <p>Two pieces deploy independently:</p> <ul> <li>The agent (not in this repo), built and deployed in the Google Cloud   console. See <code>agent/</code> for how to create it.</li> <li>The TAC server (this repo), a FastAPI app that connects Twilio's voice and   messaging channels to your agent. Deployed to Cloud Run from   <code>server/</code>.</li> </ul>"},{"location":"deploy/cx_agent_studio/#architecture","title":"Architecture","text":"<p>Voice can run cascaded, where Twilio does the speech-to-text and text-to-speech and your agent only ever sees text, or speech-to-speech, where raw call audio streams to the agent.</p> cascaded (default) speech-to-speech (<code>s2s</code>) What carries the call ConversationRelay, text over a WebSocket Media Streams, raw audio over a WebSocket Who does the STT/TTS Twilio CX Agent Studio Agent API <code>runSession</code> (text) <code>BidiRunSession</code> (audio) Voice + Conversation Orchestrator / Conversation Memory Yes / Yes No / No <p><code>server/main.py</code> picks which one you get, and switching is a one-file edit (see step 5).</p> <p>Below, blue edges are the cascaded voice path, purple speech-to-speech; black edges are common to both.</p> <pre><code>graph TB\n    Customer([\ud83d\udc64 Customer&lt;br/&gt;Phone Call / Message])\n\n    subgraph Twilio[\"\u2601\ufe0f Twilio Cloud\"]\n        Phone[\ud83d\udcf1 Phone Number&lt;br/&gt;/ RCS / WhatsApp / Chat]\n        Orchestrator[\ud83d\udcac Conversation Orchestrator]\n        Memory[\ud83e\udde0 Conversation Memory]\n    end\n\n    subgraph GCP[\"\u2601\ufe0f Google Cloud\"]\n        subgraph CloudRun[\"\ud83c\udfc3 Cloud Run\"]\n            Server[\u2699\ufe0f TAC Server]\n        end\n\n        subgraph CES[\"\ud83e\udd16 CX Agent Studio\"]\n            Agent[\ud83e\udde0 Deployed Agent]\n        end\n\n        Secret[\ud83d\udd10 Secret Manager&lt;br/&gt;Twilio credentials]\n    end\n\n    Customer --&gt;|Phone Call| Phone\n    Customer --&gt;|\"Message&lt;br/&gt;(SMS/RCS/WhatsApp/Chat)\"| Phone\n\n    %% Voice, cascaded (default): Twilio does the STT/TTS, text on the wire\n    Phone &lt;--&gt;|\"ConversationRelay&lt;br/&gt;(text over wss://)\"| Server\n    Server &lt;--&gt;|\"runSession&lt;br/&gt;(text turns)\"| Agent\n\n    %% Voice, speech-to-speech: raw audio on the wire, CX Agent Studio does the STT/TTS\n    Phone &lt;--&gt;|\"Media Streams&lt;br/&gt;(raw audio over wss://)\"| Server\n    Server &lt;--&gt;|\"BidiRunSession&lt;br/&gt;(continuous audio)\"| Agent\n\n    %% Messaging, same in both approaches\n    Phone &lt;--&gt; Orchestrator\n    Orchestrator &lt;--&gt;|\"POST /webhook\"| Server\n    Server --&gt;|\"recall&lt;br/&gt;(cascaded voice + messaging only)\"| Memory\n\n    Server -.-&gt;|reads credentials| Secret\n\n    linkStyle 2,3 stroke:#1a73e8,stroke-width:2px\n    linkStyle 4,5 stroke:#9c27b0,stroke-width:2px\n\n    style Customer fill:#e1f5ff\n    style Twilio fill:#f0f0f0\n    style GCP fill:#e8f0fe\n    style CloudRun fill:#e6f4ea\n    style CES fill:#f3e5f5\n    style Agent fill:#e1bee7\n    style Secret fill:#fff4e6</code></pre>"},{"location":"deploy/cx_agent_studio/#deployment-components","title":"Deployment Components","text":"<ul> <li>Agent - built and deployed in CX Agent Studio in the Google Cloud console; see <code>agent/</code></li> <li>Cloud Run Service - the TAC server (FastAPI); deployed from <code>server/</code></li> <li>Artifact Registry - holds the server's container image (created automatically)</li> <li>Secret Manager - holds the Twilio credentials, read by the Cloud Run service</li> </ul>"},{"location":"deploy/cx_agent_studio/#prerequisites","title":"Prerequisites","text":""},{"location":"deploy/cx_agent_studio/#local-requirements","title":"Local Requirements","text":"What Where Needed for Google Cloud CLI install docs, or <code>brew install --cask google-cloud-sdk</code> every deploy step Python 3.11 python.org only the optional debug client, <code>agent/invoke.py</code>"},{"location":"deploy/cx_agent_studio/#google-cloud-requirements","title":"Google Cloud Requirements","text":"What Where .env value Project with billing and CX Agent Studio access Cloud Console <code>GOOGLE_CLOUD_PROJECT</code> Region with Cloud Run availability any <code>GOOGLE_CLOUD_LOCATION</code> Deployed agent, Set up API access on its Deploy tab <code>agent/README.md</code> <code>CX_AGENT_ID</code> <p>The deploy scripts handle API enablement and both service accounts.</p>"},{"location":"deploy/cx_agent_studio/#twilio-requirements","title":"Twilio Requirements","text":"What Where (Twilio Console) .env value Account SID (starts <code>AC</code>) Console Dashboard <code>TWILIO_ACCOUNT_SID</code> Auth Token Develop &gt; API Key &amp; creds &gt; API Keys &amp; auth tokens &gt; Auth Tokens tab <code>TWILIO_AUTH_TOKEN</code> API Key + Secret same page, API Keys tab, Create API Key. The secret shows once, so copy it now <code>TWILIO_API_KEY</code> (starts <code>SK</code>), <code>TWILIO_API_SECRET</code> Phone number, voice-capable (also SMS-capable if you want messaging) Products &amp; services &gt; Numbers &amp; senders <code>TWILIO_PHONE_NUMBER</code> (E.164) <p>That's everything voice needs.</p>"},{"location":"deploy/cx_agent_studio/#optional","title":"Optional","text":"What Where .env value Enables Conversation Configuration Conversation Orchestrator quickstart <code>TWILIO_CONVERSATION_CONFIGURATION_ID</code> messaging and Conversation Memory; without it, voice only RCS sender Products &amp; services &gt; Numbers &amp; senders <code>TWILIO_RCS_SENDER_ID</code> the RCS channel WhatsApp sender Products &amp; services &gt; Numbers &amp; senders <code>TWILIO_WHATSAPP_NUMBER</code> (<code>whatsapp:+1555...</code>) the WhatsApp channel <p>Leave the Conversation Configuration's webhook blank for now. It needs the Cloud Run host, which you get after the first deploy (Twilio Configuration).</p> <p>To message numbers beyond your own test numbers, SMS needs a registered brand and campaign and RCS needs carrier approval. Both go through Twilio and both block end-to-end testing, so start early.</p>"},{"location":"deploy/cx_agent_studio/#quick-start","title":"Quick Start","text":""},{"location":"deploy/cx_agent_studio/#1-build-the-agent","title":"1. Build the agent","text":"<p>Build and deploy the agent in CX Agent Studio and copy its resource name, following <code>agent/README.md</code>.</p>"},{"location":"deploy/cx_agent_studio/#2-configure-environment","title":"2. Configure Environment","text":"<pre><code>cd deploy/cx_agent_studio\ncp .env.example .env\n</code></pre> <p>Edit <code>.env</code> with your values:</p> <pre><code># GCP Configuration\nGOOGLE_CLOUD_PROJECT=your-project-id\nGOOGLE_CLOUD_LOCATION=your-region\n\n# CX Agent Studio agent (from the console; location is the CX Agent Studio `us` multi-region)\nCX_AGENT_ID=projects/your-project/locations/us/apps/your-app-id\n\n# Twilio Credentials\nTWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\nTWILIO_AUTH_TOKEN=your_auth_token\nTWILIO_API_KEY=SKxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\nTWILIO_API_SECRET=your_api_secret\n\n# Twilio Configuration\nTWILIO_PHONE_NUMBER=+1234567890\nTWILIO_CONVERSATION_CONFIGURATION_ID=conv_configuration_xxxx\n\n# Optional: set either one to enable that messaging channel\nTWILIO_RCS_SENDER_ID=rcs_sender_xxxxxxxxxxxxxxxxxx\nTWILIO_WHATSAPP_NUMBER=whatsapp:+1234567890\n</code></pre> <p>Console paths for all of these are in Twilio Requirements. The four credentials (<code>TWILIO_ACCOUNT_SID</code>, <code>TWILIO_AUTH_TOKEN</code>, <code>TWILIO_API_KEY</code>, <code>TWILIO_API_SECRET</code>) go into Secret Manager; the rest are passed as env vars.</p>"},{"location":"deploy/cx_agent_studio/#3-authenticate-one-time","title":"3. Authenticate (one-time)","text":"<p>Run once per machine; the credentials persist for future deploys. The project comes from <code>.env</code>, so you don't need <code>gcloud config set project</code>.</p> <pre><code>gcloud auth login                        # gcloud CLI (server deploy)\ngcloud auth application-default login    # ADC (local invoke.py)\n</code></pre>"},{"location":"deploy/cx_agent_studio/#4-create-the-deploy-service-account-one-time","title":"4. Create the deploy service account (one-time)","text":"<p>Creates the <code>tac-deployer</code> SA, derived from your project, with nothing to configure. The deploy grants it the roles it needs.</p> <pre><code>make create-sa\n</code></pre>"},{"location":"deploy/cx_agent_studio/#5-optional-switch-voice-approach","title":"5. (Optional) Switch voice approach","text":"<p><code>server/main.py</code> defaults to cascaded. For speech-to-speech, open it and follow the comments: comment out the \"cascaded\" block, uncomment the \"s2s\" block. Speech-to-speech voice supports neither Conversation Orchestrator nor Conversation Memory. Messaging keeps both either way.</p>"},{"location":"deploy/cx_agent_studio/#6-deploy","title":"6. Deploy","text":"<pre><code>make deploy-all\n</code></pre> <p><code>deploy-all</code> runs <code>deploy-secret</code>, then <code>deploy-server</code>. You can also run those individually.</p> <p>Deployment output:</p> <pre><code>\u2705 Deployed: https://tac-cx-server-xxxxx.&lt;region&gt;.run.app\n\nConfigure Twilio:\n  Conversation Orchestrator status callback : https://tac-cx-server-xxxxx.&lt;region&gt;.run.app/webhook   (POST)\n  Voice \"A call comes in\" webhook           : https://tac-cx-server-xxxxx.&lt;region&gt;.run.app/twiml     (POST)\n</code></pre> <p>Copy those webhook URLs for the next section. Re-running <code>make deploy-server</code> keeps the same URL, including after switching cascaded/s2s.</p>"},{"location":"deploy/cx_agent_studio/#twilio-configuration","title":"Twilio Configuration","text":""},{"location":"deploy/cx_agent_studio/#configure-voice-webhook-phone-number","title":"Configure Voice Webhook (Phone Number)","text":"<ol> <li>Go to Products &amp; services &gt; Numbers &amp; senders</li> <li>Select your phone number</li> <li>Under Voice configuration, set the primary handler:</li> <li>A CALL COMES IN: Webhook</li> <li>URL: the <code>/twiml</code> URL from the deploy output</li> <li>HTTP Method: POST</li> <li>Save</li> </ol> <p>Both voice approaches use this same <code>/twiml</code> webhook, so there's nothing to change here when you switch.</p>"},{"location":"deploy/cx_agent_studio/#configure-conversation-webhook-messaging","title":"Configure Conversation Webhook (Messaging)","text":"<ol> <li>Go to Products &amp; services &gt; Conversation Orchestrator &gt; Conversation Configurations</li> <li>Select your Conversation Configuration and click Edit</li> <li>Under Webhook, set the callback URL to the <code>/webhook</code> URL from the deploy    output, method POST</li> <li>Save</li> </ol> <p>If you created the configuration via the API with <code>statusCallbacks</code> already pointing at your Cloud Run URL, this is done already.</p> <p>Messaging flows through the Conversation Orchestrator status callback, not the phone number's \"A message comes in\" webhook. Setting that one does nothing here. This single webhook covers SMS, Chat, RCS, and WhatsApp.</p>"},{"location":"deploy/cx_agent_studio/#view-logs","title":"View Logs","text":""},{"location":"deploy/cx_agent_studio/#cloud-run-logs","title":"Cloud Run Logs","text":"<p>Application logs (startup, agent calls, errors):</p> <pre><code>gcloud run services logs read tac-cx-server --project your-project-id --region your-region --limit 50\n</code></pre> <p>Or go to the Cloud Logging Console, and filter by Cloud Run Revision \u2192 <code>tac-cx-server</code>.</p>"},{"location":"deploy/cx_agent_studio/#request-logs","title":"Request Logs","text":"<p>To confirm whether Twilio's webhook is reaching the service (and with what status code):</p> <pre><code>gcloud logging read \\\n  'resource.type=\"cloud_run_revision\" AND resource.labels.service_name=\"tac-cx-server\" AND httpRequest.requestUrl!=\"\"' \\\n  --project your-project-id --limit 25 --freshness=1h \\\n  --format='table(timestamp, httpRequest.requestMethod, httpRequest.status, httpRequest.requestUrl)'\n</code></pre>"},{"location":"deploy/cx_agent_studio/#troubleshooting","title":"Troubleshooting","text":"Symptom Cause Every webhook 403s <code>TWILIO_AUTH_TOKEN</code> doesn't belong to <code>TWILIO_ACCOUNT_SID</code>. Usually the token was rotated without re-running <code>make deploy-secret</code>, or the two come from different (sub)accounts. The request log query shows these. Calls connect, nobody speaks <code>TWILIO_VOICE_PUBLIC_DOMAIN</code> was overridden in <code>.env</code>. The deploy sets it to the Cloud Run host, so an override points Twilio's <code>wss://</code> at the wrong place. Cloud Run logs show no WebSocket connection at all. Messages reach Twilio, agent never replies No <code>TWILIO_CONVERSATION_CONFIGURATION_ID</code> set, so messaging was never enabled; or the configuration's webhook isn't <code>/webhook</code>; or the channel has no capture rule in the Conversation Configuration (WhatsApp is the common miss); or the channel isn't registered in <code>server/main.py</code>, in which case it's dropped silently. RCS/WhatsApp works for your number only The approval gate, not a bug. See Twilio Requirements."},{"location":"deploy/cx_agent_studio/#update-code","title":"Update Code","text":"<ul> <li>After editing the agent: rebuild/redeploy it in CX Agent Studio.</li> <li>After editing the server or the package (including switching cascaded/s2s): <code>make deploy-server</code>.</li> <li>After rotating Twilio credentials in <code>.env</code>: <code>make deploy-secret</code>.</li> </ul>"},{"location":"deploy/cx_agent_studio/agent/","title":"CX Agent Studio agent","text":"<p>This folder exists to get you two things before you deploy the server:</p> <ol> <li>A working agent, and its <code>CX_AGENT_ID</code>. That resource name is the one    agent-side value <code>../.env</code> needs. Build the agent and    Get the agent ID below.</li> <li>Confidence the agent itself works. <code>invoke.py</code> chats with it from your    terminal, so you can rule the agent in or out before adding Twilio to the    picture. See Debugging with invoke.py.</li> </ol> <p>Nothing here is deployed or imported. The agent is built and deployed in the Google Cloud console (CX Agent Studio is the low-code visual builder there for Customer Engagement Suite agents), so this folder has no agent source and no deploy script. The Twilio Agent Connect (TAC) server (<code>server/main.py</code>) is the part that deploys, to Cloud Run, and it connects Twilio to the agent over the agent's API.</p>"},{"location":"deploy/cx_agent_studio/agent/#build-the-agent","title":"Build the agent","text":"<ol> <li>In the console, open Gemini Enterprise for CX \u2192 CX Agent Studio \u2192 Agents    and click Create agent.</li> <li>Give it a name, set the instructions, pick a model, and add any    tools (Google Search, data stores, connectors like Salesforce, MCP).</li> <li>Use the Preview tab to chat-test the agent.</li> <li>Open the Deploy tab. On \"How do you want to deploy your agent?\" choose    Set up API access (the TAC server calls the agent's API; you do not need    the web widget or the \"Connect to a platform\" telephony integration).</li> </ol>"},{"location":"deploy/cx_agent_studio/agent/#get-the-agent-id","title":"Get the agent ID","text":"<p>Set up API access shows a sample <code>runSession</code> request. Copy the agent (app) resource name from it, the part before <code>/sessions/</code>, which looks like:</p> <pre><code>projects/&lt;project&gt;/locations/us/apps/&lt;app-id&gt;\n</code></pre> <p>(CX Agent Studio agents live in the <code>us</code> multi-region.) Put it in <code>../.env</code> as <code>CX_AGENT_ID</code>:</p> <pre><code>CX_AGENT_ID=projects/your-project/locations/us/apps/your-app-id\n</code></pre>"},{"location":"deploy/cx_agent_studio/agent/#debugging-with-invokepy","title":"Debugging with invoke.py","text":"<p><code>invoke.py</code> is a separate, simpler debugging tool. It talks to the agent over the CX Agent Studio text <code>runSession</code> API (<code>POST https://ces.googleapis.com/v1/&lt;CX_AGENT_ID&gt;/sessions/&lt;session-id&gt;:runSession</code>) so you can sanity-check the agent's own logic/responses from a terminal, independent of the voice/audio path. It doesn't exercise <code>BidiRunSession</code> or audio at all.</p> <p>Authentication uses Application Default Credentials. The Cloud Run service account needs the <code>roles/ces.client</code> role (granted by <code>server/deploy.sh</code>). To run <code>invoke.py</code> locally, log in first so ADC has credentials to use:</p> <pre><code>gcloud auth application-default login\n</code></pre>"}]}