# GCP Configuration (required for deployment)
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# Agent Platform Runtime (Reasoning Engine) - for runtime/ folder
GCP_REASONING_ENGINE_ID=your-reasoning-engine-id

# Dialogflow CX - for dialogflow/ folder
DIALOGFLOW_CX_AGENT_ID=your-agent-uuid

# Optional: Path to service account key (uses ADC if not set)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
