# Connectors

Connectors combine a Google Cloud agent runtime integration with TAC channel
management. Import them from `tac_google.connectors` — the top-level
`tac_google` package deliberately exposes only `__version__`, so installing
without extras never fails on import.

::: tac_google.connectors
    options:
      show_submodules: false
